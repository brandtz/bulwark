# Handoff — W4-2 E2E coverage backfill (2026-05-16)

**Slice:** W4-2 (deferred E2E specs from W2-5, W3-2, W3-4, W3-5)
**Status:** Tests-only slice. 10 specs + 2 seed helpers shipped. vue-tsc clean, all specs discoverable.

## What shipped

### Seed helpers (real-backend only; no-op when `BULWARK_BACKEND !== 'real'`)

| File | Purpose |
|---|---|
| [tests/setup/seed-sub-portal.ts](tests/setup/seed-sub-portal.ts) | Idempotent `subcontractor_users` join row linking `jeff@bulwark.demo` → `Roof King Co.`. Existing seed WO `WO-2026-0001` already assigns slot-roofing to that sub, so `listMyAssignments(jeff)` returns it once the join row exists. |
| [tests/setup/seed-homeowner-portal.ts](tests/setup/seed-homeowner-portal.ts) | Idempotent homeowner persona (`homer@bulwark.demo`, password `BulwarkDemo!1`) + membership (role `homeowner`) + `homeowner_users` row linked to the accepted seed property (which already carries the seed quote + invoices). |

Both helpers use plain postgres-js + raw SQL (same pattern as `scripts/db-seed.mjs`) and short-circuit when `BULWARK_BACKEND !== 'real'`.

### Specs created

| # | File | Status | Reason for skip |
|---|---|---|---|
| 1 | [tests/e2e/dashboard.spec.ts](tests/e2e/dashboard.spec.ts) | **active** | — |
| 2 | [tests/e2e/reports-csv.spec.ts](tests/e2e/reports-csv.spec.ts) | **active** | — |
| 3 | [tests/e2e/global-search.spec.ts](tests/e2e/global-search.spec.ts) | skipped | `SearchPalette` UI + Cmd-K topbar listener not yet in source (W3-5 handoff "Deferred — Presentation/UX layer"). |
| 4 | [tests/e2e/saved-views.spec.ts](tests/e2e/saved-views.spec.ts) | skipped | Saved-views dropdown UI on `/admin/properties` not yet shipped (W3-5 handoff deferred section). |
| 5 | [tests/e2e/sub-portal.spec.ts](tests/e2e/sub-portal.spec.ts) | **active (with trailing skip)** | Landing + assigned WO assertions run; the "complete a slot" tail is `test.skip` because `/sub/work-orders/[id]` isn't a page yet (W3-4 follow-up). |
| 6 | [tests/e2e/sub-coi-upload.spec.ts](tests/e2e/sub-coi-upload.spec.ts) | **active** | — |
| 7 | [tests/e2e/homeowner-portal.spec.ts](tests/e2e/homeowner-portal.spec.ts) | **active (with trailing skip)** | Landing + linked-property assertions run; "open a quote, Accept CTA" tail is `test.skip` because `/homeowner/quotes/[id]` isn't shipped (W3-4 handoff "Known follow-ups"). |
| 8 | [tests/e2e/homeowner-invoice.spec.ts](tests/e2e/homeowner-invoice.spec.ts) | skipped | `/homeowner/invoices/[id]` detail page not shipped (W3-4 follow-up — detail pages are stubs). |
| 9 | [tests/e2e/settings-permissions.spec.ts](tests/e2e/settings-permissions.spec.ts) | skipped | `/settings/permissions` matrix UI is W2-5 known debt; service + contract live, UI scheduled for W4-1. |
| 10 | [tests/e2e/mfa.spec.ts](tests/e2e/mfa.spec.ts) | skipped | `/profile/security` enroll/confirm/backup-codes UI not shipped (W2-5 known debt). Uses `TOTP` from `otpauth` (already a dep). |

All 10 files carry the ADR-0008 rich-comment header. Each spec ≤ 80 LOC. Each spec is independently runnable via `pnpm exec playwright test tests/e2e/<file> --project=chromium`.

### `data-test` attributes added

**None.** Every UI target the active specs need was already wired with `data-testid` hooks by W2-5, W3-2, W3-4. Surgical attribute adds were unnecessary for this slice.

The skipped specs reference forward-looking `data-testid` names (`global-search-input`, `permissions-matrix`, `mfa-qr-image`, etc.) that the respective UI authors should attach when those screens ship.

## Verification

- `pnpm exec vue-tsc --noEmit` → exit 0 (clean).
- `pnpm exec playwright test --list` → exit 0; **534 tests in 72 files** discovered. The 10 new specs all appear under the chromium project (and replicated across mobile-safari + mobile-chrome, where they self-skip in `beforeEach` per the desktop-only convention used by `property-depth.spec.ts` etc.).
- Per-file `get_errors` clean for all 12 new files (10 specs + 2 seed helpers).

## How to run

```
# Terminal A
pnpm dev

# Terminal B (real backend default — seeds via global-setup)
pnpm exec playwright test tests/e2e/dashboard.spec.ts --project=chromium
pnpm exec playwright test tests/e2e/reports-csv.spec.ts --project=chromium
pnpm exec playwright test tests/e2e/sub-portal.spec.ts --project=chromium
pnpm exec playwright test tests/e2e/sub-coi-upload.spec.ts --project=chromium
pnpm exec playwright test tests/e2e/homeowner-portal.spec.ts --project=chromium
```

The four `test.skip(true, ...)` specs will be reported as skipped but won't fail.

## Blockers / follow-ups

- **W4-1 (or whoever ships the UIs)** can unskip each of the four fully-skipped specs by:
  1. Wiring the data-testid hooks listed in each spec's "Planned flow" comment block.
  2. Removing the leading `test.skip(true, '...')` call.

- The seed helpers depend on `DATABASE_URL` + the W3-4 migrations (`subcontractor_users`, `homeowner_users`) being applied. Both should be present in any environment running `BULWARK_BACKEND=real`; global-setup throws otherwise.

- The dashboard range-picker assertion uses `expect.poll` over all 8 KPI cards because seed values can be identical for some range pairs. The 30d→90d swap reliably moves at least the revenue + AR aging cards in the current seed; if the seed grows past the 90d cutoff this assertion may need to widen the window pair.

## Constraints honoured

- ✅ No `/demo/` edits.
- ✅ No service, contract, schema, or business-logic changes — tests + seed helpers only.
- ✅ No new npm dependencies. `otpauth`, `qrcode`, `bcryptjs`, `postgres` all pre-existing.
- ✅ Every new file ≥ 40 LOC carries the ADR-0008 rich-comment header.
- ✅ Specs use `data-testid` (no class selectors).
- ✅ Skipped specs carry a TODO + reason.
