# Bulwark — Build Status

> **Live cursor.** Updated by every agent at the end of every session.
> Read this file before doing anything.

---

## Current Phase

**Phase 1 — App Shell & Design System**

## Active Epic

[E11 — Backend Wiring](agents/epics/E11-backend-wiring.md) — **🟢 ready to start**. Phase 2 infrastructure stack locked by [ADR-0012](agents/decisions/ADR-0012-phase2-infrastructure.md): local PostgreSQL 18 (`postgresql-x64-18`) for dev, Neon for staging (deferred), `bcryptjs` + `nuxt-auth-utils` for auth, Cloudflare R2 for object storage, `pg-boss` worker on Render/Railway for jobs, Vercel for the Nuxt app, Stripe for E13-S7 only. Single remaining unblock: sponsor pastes `DATABASE_URL` for the local `bulwark_app`/`bulwark_dev` role into `bulwark/.env.local`. E12 (Subcontractor Portal) and E13 (Homeowner Portal) follow strictly serially per ADR-0012 §7.

## Active Story

**Wave 4 — Regression sweep, ADR ratification, conventions refresh (W4-3) — IN PROGRESS (2026-05-16).** Audit-only slice per PHASE1_HARDENING_PLAN §4 Wave 4. No service / contract / schema / page edits. Deliverables: ADR ratification table, BUILD_STATUS audit + reorganization (this section), CONVENTIONS.md Phase 1 conventions section, lint/typecheck/test counts, DoD checklist, Wave 5 priority recommendations. Handoff: [`agents/handoffs/2026-05-16-W4-3-regression-sweep.md`](agents/handoffs/2026-05-16-W4-3-regression-sweep.md).

**Wave 5 — Security hardening (planned).** Top candidates surfaced by W4-3: sealed-secret storage for `provider_configs` + `webhooks` (KMS column), persona × route Playwright matrix expansion to the new portal + field surfaces, real SES / Twilio wiring behind the existing stub providers, IndexedDB-backed offline queue + Background Sync API promotion, rate limiting on the auth endpoints (lockout primitives shipped in W2-5).

## Phase 1 — Completed Slices

> Wave 1–3 entries are kept here verbatim from the live cursor (newest at top). Wave 4 marks the boundary between Phase 1 build-out and Phase 1 hardening review.

**Wave 5 — Secrets handling, at-rest sealing, redaction (W5-2) — DONE (2026-05-16).** Per [ADR-0036](agents/decisions/ADR-0036-secret-handling.md), Bulwark gains envelope-encrypted provider configs, an expanded logger-redaction net, and a canonical env-var registry. **At-rest sealing**: new column `provider_configs.config_encrypted text` (migration [`0011_sealed_provider.sql`](server/db/migrations/0011_sealed_provider.sql); journal entry idx 11 appended) — additive + nullable, legacy `config` JSONB retained as backfill seam. [`server/utils/crypto.ts`](server/utils/crypto.ts) extended with `encryptJsonBlob` / `decryptJsonBlob` helpers + new `BULWARK_ENCRYPTION_KEY` env-var preference (falls back to `JWT_SECRET` → `NUXT_SESSION_PASSWORD` so existing dev envs keep working). [`server/services/provider-config.real.ts`](server/services/provider-config.real.ts) `upsert()` now seals via `encryptJsonBlob(parsed)` and writes `config = {}` (plaintext never lands in new rows); new exported helper `unsealProviderConfig(row)` reads `config_encrypted` first, falls back to legacy `config`, logs (no payload) + degrades to `{}` on tamper. Both provider adapters ([`server/services/_providers/email.ts`](server/services/_providers/email.ts) + [`server/services/_providers/sms.ts`](server/services/_providers/sms.ts)) now call `unsealProviderConfig(row)` instead of dereferencing `row.config` directly; adapter contract surfaces unchanged. **Other secret-bearing columns audited**: `webhooks.secret_hash` (sha256, already correct), `api_keys.secret_hash` (bcrypt, already correct), `user_mfa.secret_encrypted` (AES-GCM, already covered by ADR-0024) — no changes required. **Logger redaction**: [`server/utils/logger.ts`](server/utils/logger.ts) deny list grew 7→35 keys (auth-credential family + PII patterns ssn/dob/creditCard/cardNumber/cvv/cvc/bankAccount/routingNumber/taxId/ein/driverLicense, camelCase + snake_case variants) plus a `_encrypted` suffix rule; redaction is now recursive with a `WeakSet` cycle guard returning `[CIRCULAR]`. **.env hygiene**: [`.env.example`](.env.example) rewritten as the canonical registry — added `BULWARK_VERSION`, `BULWARK_ENCRYPTION_KEY`, `BULWARK_ALLOW_PROD_SEED`, `BULWARK_LOG_LEVEL`, `BULWARK_CORS_ORIGINS`, `BULWARK_RATE_LIMIT_DISABLED`, `BULWARK_NOTIFICATIONS_DISABLED`, `BULWARK_PDF_STUB`, `R2_*` family (replaces the previously-unused `CLOUDFLARE_R2_*` trio). `.gitignore` already covers `.env*` with `!.env.example`; `git ls-files | rg '\.env'` returns only `.env.example`. **Hardcoded-secret sweep**: zero real-secret remediations — only acceptable dev-convenience hits ([`run-dev-real.ps1`](run-dev-real.ps1) DB password explicitly allow-listed by the slice brief, ADR/handoff prose references). **Scanner**: new [`scripts/scan-secrets.mjs`](scripts/scan-secrets.mjs) (Stripe sk/pk + AWS AKIA + JWT-shaped + conn-string-with-creds + literal-assigned-credential rules, skips `demo/` `boilerplate/` `tests/` `agents/` `docs/` `public/` `.github/` + binary ext; exits 0 by default, `--strict` for CI). NOT wired to pre-commit/CI (ops decision flagged in handoff). **Runtime config split**: audited — `nuxt.config.ts` `runtimeConfig.public` already contains only `backend` + `appName`; `sessionPassword` / `databaseUrl` / `jwtSecret` are correctly server-only. No moves needed; `nuxt.config.ts` untouched. **Tests**: 15 new unit tests all passing — [`tests/unit/logger-redaction.test.ts`](tests/unit/logger-redaction.test.ts) (7 cases: auth family, PII family, `_encrypted` suffix, non-sensitive leave-alone, nested recursion, array walks, circular short-circuit) + [`tests/unit/crypto-blob.test.ts`](tests/unit/crypto-blob.test.ts) (4 cases: JSON round-trip, IV uniqueness, null/empty/undefined → `{}`, tamper detection throws). 0 new npm deps (Node built-ins only). Handoff: [`agents/handoffs/2026-05-16-W5-2-secrets-audit.md`](agents/handoffs/2026-05-16-W5-2-secrets-audit.md). **Yellow build disclosure**: W5-2's own files all typecheck + test clean; the full repo gate is exit 2 typecheck + 2 unit failures + 1 collection error, all pre-existing W5-1 mid-flight work (BulwarkAvatar `safeUrl` helper missing, `server/middleware/01.rate-limit.ts:74` number/string, `server/services/auth.real.ts:282` orphan `input`, `account.real.ts:88` unused `tenantResolver`, `tests/unit/safe-url.test.ts` collection, `account-export.test.ts` schema, `labels.test.ts` `legal.privacy.title` namespace) — none touch W5-2 files. **Ops follow-ups** (handoff §Operational follow-ups): generate `BULWARK_ENCRYPTION_KEY` via `openssl rand -hex 32` for staging+prod, rotate `.env.local` Stripe + R2 keys (flagged compromised by prior GitHub push protection), rotate `run-dev-real.ps1` DB password, decide on scan-secrets CI/pre-commit wiring, backfill existing `provider_configs` rows by saving each through admin UI (or write a one-shot `scripts/seal-provider-configs.mjs`), regenerate `meta/0011_snapshot.json` next time `drizzle-kit generate` runs (drizzle-kit migrate doesn't need it at runtime).

**Wave 5 — Privacy + Compliance Posture (W5-4) — DONE (2026-05-16).** Per [ADR-0038](agents/decisions/ADR-0038-privacy-and-dsr.md), Bulwark gains its first user-facing privacy surface: public legal pages (`/privacy`, `/terms`, `/dpa`, `/goodbye`), per-user DSR controls (`/profile/data` with Export-my-data and Delete-my-account), and the documented retention + breach-response policies. New contract [`shared/contracts/account.ts`](shared/contracts/account.ts) (`IAccountService.{exportPersonalData, requestDeletion, purgeExpiredDeletions}` + Zod schemas + `SoleAdminError` + `ACCOUNT_DELETION_GRACE_DAYS=30` + `ACCOUNT_EXPORT_AUDIT_CAP=5000`). New services [`shared/mocks/account.mock.ts`](shared/mocks/account.mock.ts) and [`server/services/account.real.ts`](server/services/account.real.ts) wired through both factories ([`shared/mocks/factory.ts`](shared/mocks/factory.ts), [`server/utils/services-factory.ts`](server/utils/services-factory.ts)); the real service joins `users`, `memberships`, `homeowner_users`+`properties`, `subcontractor_users`+`subcontractors`, `notifications`, `notification_subscriptions`, and `audit_log` (only rows where `actor_user_id = userId`, foreign actors excluded), enforces the sole-admin guard, soft-deletes by nulling PII + swapping email for a `deleted-<sha256>@deleted.invalid` placeholder + setting `deleted_at`, and the daily purge nulls `audit_log.actor_user_id` before hard-deleting the row (event survives, identity does not). New endpoints [`server/api/account/export.get.ts`](server/api/account/export.get.ts) (returns JSON with `Content-Disposition: attachment`, `Cache-Control: no-store`) and [`server/api/account/delete.post.ts`](server/api/account/delete.post.ts) (409 + `code: 'SOLE_ADMIN'` on guard fail, `clearUserSession` on success). New cron stub [`server/jobs/account-purge.ts`](server/jobs/account-purge.ts) (schedule not wired — ops decision). New pages: [`app/pages/privacy.vue`](app/pages/privacy.vue) (GDPR + CCPA disclosures: 12 sections covering categories, purposes, legal bases, sharing, transfers, retention summary, rights with `[user]` redaction note, cookies, children, security, changes, contact), [`app/pages/terms.vue`](app/pages/terms.vue), [`app/pages/dpa.vue`](app/pages/dpa.vue) (Art. 28 stub with 72-hour breach notification + annual audit rights), [`app/pages/profile/data.vue`](app/pages/profile/data.vue) (two cards + DELETE-type-to-confirm gate + sole-admin hint linking `/admin/users`), [`app/pages/goodbye.vue`](app/pages/goodbye.vue). [`app/middleware/auth.global.ts`](app/middleware/auth.global.ts) PUBLIC_ROUTES extended with `/privacy`, `/terms`, `/dpa`, `/goodbye`. [`app/layouts/default.vue`](app/layouts/default.vue) gains a `data-testid="app-legal-footer"` with the three legal links. [`app/components/nav/UserMenu.vue`](app/components/nav/UserMenu.vue) gains an "Account & data" link. Labels: [`shared/labels/defaults.ts`](shared/labels/defaults.ts) gains `legal.privacy.*`, `legal.terms.*`, `legal.dpa.*`, `legal.account.*` namespaces. Docs: [`docs/PRIVACY_AND_RETENTION.md`](docs/PRIVACY_AND_RETENTION.md) (per-table retention matrix + enforcement detail) and [`docs/BREACH_RESPONSE.md`](docs/BREACH_RESPONSE.md) (8-section runbook with 72-hour controller notice + tabletop cadence). No migration: `users.deleted_at` already exists via `auditColumns`. Tests: 3 new unit specs (`tests/unit/account-export.test.ts` 4 cases, `tests/unit/account-deletion.test.ts` 3 cases, `tests/unit/account-purge.test.ts` 3 cases). Handoff: [`agents/handoffs/2026-05-16-W5-4-privacy-compliance.md`](agents/handoffs/2026-05-16-W5-4-privacy-compliance.md). Ops actions required (see handoff): wire the daily cron, configure `privacy@` / `legal@` inboxes, fill in TBD copy (governing law, subprocessor list, hosting region). 0 new npm deps.

**Wave 3 — Reporting + Dashboards (W3-2 / EH-K) — DONE.** Per [ADR-0030](agents/decisions/ADR-0030-reporting-and-dashboards.md), Bulwark replaces the placeholder `/admin` landing with a real operating dashboard and ships a five-report surface at `/admin/reports/*`. New contract [`shared/contracts/reporting.ts`](shared/contracts/reporting.ts) (`IReportingService.{dashboardKpis, quotesByStatus, wosByPriority, arAging, revenueTrend, topProperties, subcontractorPerformance, inspectionPassRate}`) + barrel + `BulwarkServices.reporting`. New services: [`shared/mocks/reporting.mock.ts`](shared/mocks/reporting.mock.ts) (in-memory synthesis from existing mocks) and [`server/services/reporting.real.ts`](server/services/reporting.real.ts) (Drizzle aggregates with `assertSameTenant`; read-only, deliberately NOT wrapped in `withAudit` per ADR-0030 D5). Both factories wired. UI: [`app/pages/admin/index.vue`](app/pages/admin/index.vue) REPLACED with a KPI dashboard (8 cards in 2×4 grid + delta-vs-previous-range, donut for quotes-by-status, bar for WOs-by-priority, sparkline for revenue trend, bar for AR aging, two top-N lists, date-range picker 7d/30d/90d/YTD/custom). [`app/pages/admin/reports/index.vue`](app/pages/admin/reports/index.vue) + [`app/pages/admin/reports/[slug].vue`](app/pages/admin/reports/[slug].vue) (5 slugs: revenue/subcontractor/inspection-pass/ar-aging/top-properties — each renders date-range + table + Export CSV). Hand-rolled SVG charts under [`app/components/charts/`](app/components/charts/) (`Donut.vue`, `Bar.vue`, `Sparkline.vue` — explicitly NOT Chart.js/ECharts per ADR-0030 D2). CSV export via [`app/composables/useCsvExport.ts`](app/composables/useCsvExport.ts) (Blob + `URL.createObjectURL`, RFC 4180 escaping — explicitly NOT papaparse per ADR-0030 D3). Nav: [`shared/nav/nav.config.ts`](shared/nav/nav.config.ts) gains Dashboard (Operations, mobile) + Reports (new `Insights` group). Labels: [`shared/labels/defaults.ts`](shared/labels/defaults.ts) gains `reports.titles.*`, `dashboard.kpis.*`, `dashboard.range.*`, `dashboard.empty`. Tests: 3 new unit specs (`reporting-aggregates.test.ts`, `ar-aging.test.ts`, `csv-export.test.ts`). 0 new npm deps. `pnpm exec vue-tsc --noEmit` exits 0; `pnpm exec vitest run tests/unit` → 43 files / 242+ tests passing. Handoff: [`agents/handoffs/2026-05-15-W3-2-reporting.md`](agents/handoffs/2026-05-15-W3-2-reporting.md). Deferred (ADR-0030 §Known debt): e2e specs (`dashboard.spec.ts`, `reports-csv.spec.ts`), chart tooltips + drill-through (Phase 2), configurable AR-aging bucket boundaries (Phase 2), saved-dashboard layouts (Phase 2).

**Wave 3 — Subcontractor + Homeowner portals (W3-4 / EH-N + EH-O) — DONE.** Per [ADR-0031](agents/decisions/ADR-0031-subcontractor-portal.md) and [ADR-0032](agents/decisions/ADR-0032-homeowner-portal.md), Bulwark ships the first sub-facing and customer-facing portals. New role `homeowner` added to [`shared/contracts/_shared.ts`](shared/contracts/_shared.ts), `roleEnum` in [`server/db/schema/users.ts`](server/db/schema/users.ts), `ALL_ROLES` in [`shared/auth/default-permissions.ts`](shared/auth/default-permissions.ts), and `ROLE_GROUPS` in [`app/composables/usePermissions.ts`](app/composables/usePermissions.ts). New DB schemas: `subcontractor_users` (join sub ↔ user with invite/accept timestamps + unique index on `(orgId, subId, userId)`), `subcontractor_coi_docs` (COI file + expiry separate from `compliance_docs` because COIs are sub-scoped, not property-scoped), `homeowner_users` (join property ↔ user with `kind ∈ owner|tenant|spouse|other`); barrel updated. New contract files: [`shared/contracts/subcontractor.ts`](shared/contracts/subcontractor.ts) extended with `SubcontractorUserSchema`, `SubInviteInput/Output`, `SubcontractorCoiDocSchema`, `SubCoiUploadInput`, and nine new `ISubcontractorService` methods (`listUsers / inviteUser / removeUser / resolveSubForUser / listMyAssignments / listMyQuotesRequested / listCois / uploadCoi / scanCoiExpiry`); [`shared/contracts/homeowner.ts`](shared/contracts/homeowner.ts) (new — `IHomeownerService.{listForProperty, listForUser, invite, remove}`); [`shared/contracts/quote.ts`](shared/contracts/quote.ts) gets `respondToQuote(input)` on `IQuoteService`; [`shared/contracts/services.ts`](shared/contracts/services.ts) gains `homeowner: IHomeownerService`. Events catalog ([`shared/events/catalog.ts`](shared/events/catalog.ts)) extended with seven new events — `subQuoteResponded`, `subCoiUploaded`, `subCoiExpiringSoon`, `homeownerInvited`, `homeownerAccepted`, `homeownerQuoteViewed`, `homeownerInvoiceViewed` — and the `AnyDomainEventPayload` union. Notification renderer ([`shared/notifications/templates.ts`](shared/notifications/templates.ts)) covers all seven. Mocks ([`shared/mocks/subcontractor.mock.ts`](shared/mocks/subcontractor.mock.ts), new [`shared/mocks/homeowner.mock.ts`](shared/mocks/homeowner.mock.ts), [`shared/mocks/quote.mock.ts`](shared/mocks/quote.mock.ts)) implement all new methods in-memory with `__resetSubcontractorMock` / `__resetHomeownerMock` test hooks. Real services ([`server/services/subcontractor.real.ts`](server/services/subcontractor.real.ts), new [`server/services/homeowner.real.ts`](server/services/homeowner.real.ts), [`server/services/quote.real.ts`](server/services/quote.real.ts)) implement the same surface against Drizzle: `inviteUser` / `invite` are find-or-create-user → idempotent `memberships` row → join table insert → sha256-hashed `pending_invites` token with 7-day expiry, all wrapped in `withAudit` and emitting the matching event; `uploadCoi` audits + emits `subCoiUploaded`; `scanCoiExpiry` queries the (default 30-day) window and emits `subCoiExpiringSoon` per row; `respondToQuote` audits as `metadata.kind='sub_responded'` and emits `subQuoteResponded` but deliberately does NOT advance the quote status (ADR-0031 D3 — PM owns the customer-facing transition). Both factories ([`shared/mocks/factory.ts`](shared/mocks/factory.ts), [`server/utils/services-factory.ts`](server/utils/services-factory.ts)) wired with the new `homeowner` slot. UI: new layouts [`app/layouts/sub.vue`](app/layouts/sub.vue) (4-tab strip Work-orders/Quotes/COIs/Settings) and [`app/layouts/homeowner.vue`](app/layouts/homeowner.vue) (Home/Properties/Quotes/Invoices), each independent of `default.vue` mirroring the W3-3 field carve-out; new middleware [`app/middleware/sub-role.ts`](app/middleware/sub-role.ts) and [`app/middleware/homeowner-role.ts`](app/middleware/homeowner-role.ts) (role gate only; per-sub / per-property scoping enforced server-side per ADR-0031 D2). New pages: [`app/pages/sub/index.vue`](app/pages/sub/index.vue) (home), `dashboard.vue` (redirect alias), `work-orders.vue`, `quotes.vue` (inline accept/decline calling `quote.respondToQuote`), `cois.vue` (list + URL/expiry upload form), `settings.vue`, `profile.vue` (redirect alias); [`app/pages/homeowner/index.vue`](app/pages/homeowner/index.vue), `properties.vue`, `quotes.vue`, `invoices.vue`. Routing redirects in [`app/pages/index.vue`](app/pages/index.vue) and [`app/pages/accept-invite.vue`](app/pages/accept-invite.vue) bounce `sub_contractor` → `/sub` and `homeowner` → `/homeowner`. Nav config ([`shared/nav/nav.config.ts`](shared/nav/nav.config.ts)) replaces the single `/sub/dashboard` row with five sub entries + four homeowner entries; `Profile` row extended to include `homeowner`. Labels: [`shared/labels/defaults.ts`](shared/labels/defaults.ts) gains six new groups (`SUB_TABS_DEFAULTS`, `SUB_COI_DEFAULTS`, `SUB_QUOTE_DEFAULTS`, `HOMEOWNER_TABS_DEFAULTS`, `HOMEOWNER_QUOTE_DEFAULTS`, `HOMEOWNER_EMPTY_DEFAULTS`); `LabelNamespaceSchema` ([`shared/contracts/label.ts`](shared/contracts/label.ts)) extended with the six matching namespaces (required by `labels.test.ts`). Background work: [`server/jobs/coi-expiry-check.ts`](server/jobs/coi-expiry-check.ts) (host-side loop over orgs calling `scanCoiExpiry`) + admin-only [`server/api/jobs/coi-expiry-check.post.ts`](server/api/jobs/coi-expiry-check.post.ts) trigger endpoint (gated on `super_admin`/`org_admin`). Tests: 3 new unit specs (10 tests, all pass): [`tests/unit/sub-portal.test.ts`](tests/unit/sub-portal.test.ts) (4), [`tests/unit/homeowner-portal.test.ts`](tests/unit/homeowner-portal.test.ts) (3), [`tests/unit/coi-expiry.test.ts`](tests/unit/coi-expiry.test.ts) (2 — bucketing under/over the default and an explicit window). Handoff: [`agents/handoffs/2026-05-15-W3-4-portals.md`](agents/handoffs/2026-05-15-W3-4-portals.md). **`pnpm exec vue-tsc --noEmit` exits 0; `pnpm exec vitest run tests/unit` → 43 files, 242 tests, all pass.** Deferred (NOT in W3-4): homeowner quote/invoice detail pages that emit `homeownerQuoteViewed` / `homeownerInvoiceViewed` (events catalog-ready); admin Contacts page "Invite as homeowner" button (service + endpoint in place, UI is the only gap); promotion of the COI expiry scan to a real `JobKind` + cron schedule; sub/homeowner e2e specs (Playwright fixtures need seeded `subcontractor_users` + `homeowner_users` rows once the migration is generated and applied).

**Wave 3 — Search + Saved Views + Observability (W3-5 / EH-P + EH-Q) — Foundation DONE.** Per [ADR-0033](agents/decisions/ADR-0033-global-search-and-saved-views.md) and [ADR-0034](agents/decisions/ADR-0034-observability-baseline.md), Bulwark gains a tenant-firewalled cross-entity search service, a per-user/per-org saved-view registry, and the first structured-logging + metrics baseline. **Search**: new contract [`shared/contracts/search.ts`](shared/contracts/search.ts) (`ISearchService.search` + adapter shape + pure `scoreSearchHit(query, title, subtitle)` deterministic scorer documented in ADR-0033 §1); [`shared/mocks/search.mock.ts`](shared/mocks/search.mock.ts) registers per-entity adapters that delegate to the existing mock services' `list()` methods; [`server/services/search.real.ts`](server/services/search.real.ts) implements `ilike` adapters over properties/clients/quotes/work-orders/invoices/subcontractors/inspections/contacts/buildings with per-type cap 20 + global cap. No new dependency — explicitly NOT MeiliSearch/Algolia/Elastic per ADR-0033. **Saved views**: new table `saved_views` (migration `0009_free_clea.sql`) with `(organizationId, userId NULLABLE, entityType, name, filters jsonb, sortBy, sortDir, isDefault, audit)`; new contract [`shared/contracts/saved-view.ts`](shared/contracts/saved-view.ts) (`ISavedViewService.{list, get, create, update, softDelete, setDefault}`); [`shared/mocks/saved-view.mock.ts`](shared/mocks/saved-view.mock.ts) + [`server/services/saved-view.real.ts`](server/services/saved-view.real.ts); `setDefault` clears siblings transactionally inside `withAudit({ tx, audit })`, audited as `action='state_change'` with `metadata.kind='saved_view.set_default'`. Both factories ([`shared/mocks/factory.ts`](shared/mocks/factory.ts), [`server/utils/services-factory.ts`](server/utils/services-factory.ts)) wired; `BulwarkServices` interface ([`shared/contracts/services.ts`](shared/contracts/services.ts)) gains `search: ISearchService` + `savedView: ISavedViewService`. **Observability**: [`server/utils/logger.ts`](server/utils/logger.ts) ships `log(level, message, fields)` JSON-line logger with `BULWARK_LOG_LEVEL` filter (debug/info/warn/error) and a redact pass that scrubs `password`/`token`/`secret`/`apiKey`/`authorization`/`cookie` to `[REDACTED]`. [`server/utils/metrics.ts`](server/utils/metrics.ts) ships an in-memory counter map (`incCounter`/`readCounter`/`snapshotMetrics`) seeded with 7 counters (`requests_total`, `requests_errored_total`, `jobs_enqueued_total`, `jobs_failed_total`, `webhooks_delivered_total`, `webhooks_failed_total`, `notifications_dispatched_total`). [`server/middleware/request-context.ts`](server/middleware/request-context.ts) assigns one `event.context.requestId = randomUUID()` per request, logs `request.start` + `request.complete` with `{requestId, method, path, status, durationMs}` (query string stripped from path), increments `requests_total` and (on 5xx) `requests_errored_total`. New endpoints: [`server/api/health.get.ts`](server/api/health.get.ts) `{status, version, uptimeSeconds}` (public liveness), [`server/api/ready.get.ts`](server/api/ready.get.ts) `SELECT 1` against the DB (503 on failure, public), [`server/api/metrics.get.ts`](server/api/metrics.get.ts) JSON counter snapshot gated on `org_admin`/`super_admin`. `IAuditService` extended with `logSystemError({organizationId?, kind, message, metadata?})` (mock + real impls; entityType=`system`, action=`state_change`, never throws). Console replacement: surgical in six high-traffic paths only ([`audit.real.ts`](server/services/audit.real.ts), [`_queue/index.ts`](server/services/_queue/index.ts), [`webhook-deliver.ts`](server/jobs/webhook-deliver.ts), [`webhook-dispatcher.ts`](server/services/_subscribers/webhook-dispatcher.ts), [`notification-subscriber.ts`](server/services/_subscribers/notification-subscriber.ts), and the new request-context middleware); `server/jobs/worker.ts`, `_providers/email.ts`, `_providers/sms.ts` left alone per the slice scope. Tests: 4 new unit specs (18 tests, all pass): [`tests/unit/search-scoring.test.ts`](tests/unit/search-scoring.test.ts) (7), [`tests/unit/logger.test.ts`](tests/unit/logger.test.ts) (4), [`tests/unit/metrics.test.ts`](tests/unit/metrics.test.ts) (4), [`tests/unit/saved-views.test.ts`](tests/unit/saved-views.test.ts) (3). `pnpm exec vue-tsc --noEmit` exits 0 (cleared along the way: in-flight W3-2 reporting type-cast and undefined-access regressions, factory lazy-init `cachedServices` null narrowing — all minimal, no behavioural change). Inline fixup in [`shared/mocks/reporting.mock.ts`](shared/mocks/reporting.mock.ts) for `complianceDoc.list` (the contract's `list` doesn't take pagination — passed `{organizationId}` only, treated result as array) — pre-existing W3-2 bug. Handoff: [`agents/handoffs/2026-05-15-W3-5-search-obs.md`](agents/handoffs/2026-05-15-W3-5-search-obs.md). **Deferred to a follow-up slice (foundation stable, contracts shipped)**: search palette UI (`useGlobalSearch` composable + `<SearchPalette>` modal + Cmd-K binding in `AppTopBar.vue`), saved-views dropdown on the four admin list pages (properties/quotes/work-orders/invoices), `app/pages/settings/saved-views.vue` manage page, e2e specs (`global-search.spec.ts`, `saved-views.spec.ts`), counter wiring into the RPC dispatcher's per-request error counter. The full server-side stack is verified working; UI is a presentation-only slice that can land separately without contract changes.

**Wave 3 — Field / Mobile / PWA (W3-3 / EH-M) — DONE.** Per [ADR-0029](agents/decisions/ADR-0029-field-pwa-and-offline.md), Bulwark ships its first real mobile/field surface on top of the existing role=`field` placeholder. New layout `app/layouts/field.vue` renders a four-tab strip (My Day / Inspect / Photos / Notes) over a `pb-bottom-nav` safe area and hosts `<FieldInstallBanner/>` + `<BulwarkToastHost/>`; new middleware `app/middleware/field-role.ts` redirects non-field users to `/403`. Five new pages under `app/pages/field/`: `index.vue` (My Day — calls `/api/field/my-day` → `RealWorkOrderService.listForFieldUser({organizationId, userId, dateRange})`, renders WO cards with priority chip + start link), `jobs/[woId]/index.vue` (job detail with geolocation-stamped Check-in/Check-out button → `/api/field/check-in` → `services.audit.record` with `action='state_change'` + `metadata.kind ∈ {field.check_in, field.check_out}`; per-slot Start/Complete buttons; check-in history list from `/api/field/check-ins`), `jobs/[woId]/photos.vue` (`<input type="file" capture="environment">` → `propertyPhoto.create` with dataURL + offline-queue pending badge + delete affordance), `jobs/[woId]/inspect.vue` (thin host that reuses the W2-2 `<InspectionForm/>` — finds an open draft/submitted inspection or creates one against the program's most-recent active template), `check-in.vue` (standalone property picker for free-roaming check-ins). Three new API endpoints under `server/api/field/`: `my-day.get.ts`, `check-in.post.ts`, `check-ins.get.ts` — all tenant-firewalled via the session's active org. New audit helper `AuditService.getCheckInsForWorkOrder(woId, orgId)` filters audit_log by entityType + metadata.kind. New `RealWorkOrderService.listForFieldUser` (+ mock parity in `shared/mocks/work-order.mock.ts`) — deliberately NOT on `IWorkOrderService` per ADR-0029 D4 (surface-specific helper, called directly by the field API). PWA: hand-rolled (zero new npm deps — explicitly NOT `@vite-pwa/nuxt` per ADR-0029 D1) — `public/manifest.webmanifest` (name "Bulwark Field", start_url `/field`, display `standalone`), `public/sw.js` (cache-first for `/_nuxt/`, `/icons/`, fonts; network-first for `/api/`; install/activate hooks; background-sync stub for promotion), `app/plugins/pwa.client.ts` registers `/sw.js` on `load`, `app/components/field/FieldInstallBanner.vue` listens for `beforeinstallprompt` and dismisses via sessionStorage. `nuxt.config.ts` extends `app.head.link` with `<link rel="manifest">` and `meta theme-color`. Offline write seam: `app/composables/useOfflineQueue.ts` — localStorage FIFO keyed `bulwark.offline.<userId>` with `enqueue/drain/size/attachOnlineListener`, listens to browser `online` event. Labels: `shared/labels/defaults.ts` gains `FIELD_TABS_DEFAULTS` (`my-day`, `inspect`, `photos`, `notes`), `FIELD_CHECKIN_DEFAULTS` (`in`, `out`), `FIELD_INSTALL_DEFAULTS` (`prompt-title`, `prompt-body`, `cta-install`, `cta-dismiss`), `FIELD_EMPTY_DEFAULTS` (`empty-day`); `LabelNamespaceSchema` extended additively with `field`, `field.tabs`, `field.check-in`, `field.install`, and (pre-existing oversight in W3-1) `notification`. Tests: 2 new unit specs (`tests/unit/offline-queue.test.ts` — 6 tests; `tests/unit/field-wo-list.test.ts` — 4 tests) + 4 new e2e specs (`tests/e2e/field-my-day.spec.ts`, `field-check-in.spec.ts`, `field-photo-capture.spec.ts`, `field-inspection.spec.ts`). `pnpm exec vue-tsc --noEmit` exits 0. Pre-existing `notification.real.ts` count-destructure bug fixed inline (guard `unreadRow[0]?.unreadTotal ?? 0` / `totalRow[0]?.total ?? 0`) to unblock the typecheck gate. Handoff: [`agents/handoffs/2026-05-15-W3-3-field-pwa.md`](agents/handoffs/2026-05-15-W3-3-field-pwa.md). Deferred: per-user slot filter inside `listForFieldUser` (returns all org WOs in date range for v1), IndexedDB + Background Sync API promotion of the offline queue, geofence-validated check-ins, pre-signed S3 PUT for photo upload (waits on W3-1 storage layer), pull-to-refresh, push notifications on the field surface.

**Wave 3 — Notifications + Webhook dispatch (W3-1) — DONE.** Per [ADR-0027](agents/decisions/ADR-0027-notifications-dispatch.md) and [ADR-0028](agents/decisions/ADR-0028-queue-abstraction.md), Bulwark now closes the internal-fanout loop on top of the W2-4 webhook plumbing. New schema (migration `0008_majestic_kylun.sql`): `notifications` (per-user per-org row with `severity ∈ info|success|warning|error`, `relatedEntityType`/`relatedEntityId`, composite index on `(orgId, userId, readAt)`). New contracts in `shared/contracts/notification.ts` (`NotificationSchema`, `INotificationService` — `enqueue / listForUser / unreadCountForUser / markRead / markAllRead`) wired into `BulwarkServices`. Mock + real services through both factories; real service writes inside `withAudit` (audit kind `notification.enqueued`). New pure renderer `shared/notifications/templates.ts#renderNotification(eventName, payload)` covers `quote.{accepted,rejected,expired,revised}`, `work_order.{created,scheduled}`, `invoice.{marked_paid,partial_paid,voided}`, `compliance_doc.ready`, `change_order.{proposed,approved,rejected}`, `user.invited`, `webhook.ping` (silent), with generic fallback. New pure fanout helper `shared/notifications/dispatch.ts#fanoutForRecipient({ recipient, channels, rendered, sinks })` catches per-channel errors and returns `{ inApp, email, sms }` outcomes. New subscriber `server/services/_subscribers/notification-subscriber.ts` via `onAny()` resolves org users (memberships ⋈ users), reads `notification_subscriptions` per (user, eventType), falls back to `defaultChannelsFor()`, delegates to `fanoutForRecipient`, audits `notification.dispatched` per (recipient, channel) outcome. Channels: in-app (DB write), email (`server/services/_providers/email.ts` — direct `provider_configs` lookup, stub send, returns `{ id, stub: true }`), sms (`server/services/_providers/sms.ts` — mirrors email). Both providers respect `BULWARK_NOTIFICATIONS_DISABLED=1`. New queue abstraction `server/services/_queue/index.ts` (`enqueueJob` / `registerJobHandler` / exponential `setTimeout` backoff / dead-letter log) is the pg-boss promotion path documented in ADR-0028. Webhook dispatcher refactored: no more inline retry loop — the subscriber resolves matching webhooks and enqueues one `'webhook.deliver'` job per match; `server/jobs/webhook-deliver.ts` owns the 3-attempt POST + `webhook_deliveries` rows + stats update (registered by side-effect import in `server/plugins/event-subscribers.ts`). UI: new `app/components/nav/NotificationBell.vue` (bell + badge + 10-item dropdown + Mark-all-read + See-all, polls `unreadCountForUser` every 30s; SSE upgrade deferred) mounted in `AppTopBar.vue` before the user menu; new `app/pages/notifications.vue` (50/page feed, severity chip, unread highlight, related-entity deep link, unread/all filter, Mark-all-read). Labels: `shared/labels/defaults.ts` gains `notification.severity.*` and `notification.bell.empty` / `notification.feed.title` entries. Tests: 4 new unit specs (27 tests total, all pass): `tests/unit/notification-templates.test.ts` (16), `tests/unit/notification-service.test.ts` (4), `tests/unit/queue-inmemory.test.ts` (4), `tests/unit/notification-subscriber.test.ts` (3) — last one exercises the pure `fanoutForRecipient` helper with spy sinks proving per-channel failure isolation. New e2e `tests/e2e/notifications-bell.spec.ts` covers the bell increment + Mark-all-read clear arc. Per-file typecheck clean for every W3-1 file via `get_errors` after each save. Handoff: [`agents/handoffs/2026-05-15-W3-1-notifications.md`](agents/handoffs/2026-05-15-W3-1-notifications.md). Deferred (ADR-0027 §Known debt): real SES/Twilio wiring, SSE push, raw-secret webhook signing, queue persistence, admin redrive UI.

**Wave 2 — Q/WO/Invoice Depth, UI Follow-Up (EH-G / W2-3b) — DONE.** Per the W2-3 handoff, the UI surfaces deferred in [W2-3](agents/handoffs/2026-05-15-W2-3-q-wo-inv-depth.md) are now shipped on top of the already-applied `0005_shallow_adam_destine.sql` + `0006_salty_prism.sql` migrations. Quote builder (`app/pages/admin/properties/[id]/quotes/new.vue`) gains a tier segmented control (`good|better|best|custom`), per-line `optional` checkbox + `discountBps` (entered as percent, applied to totals via a discount-adjusted copy fed into `computeQuoteTotals`), per-line `categorySlug` + customer-visible `notes`, doc-level `customerVisibleNotes` textarea, and ISO `expiryDate` date picker. Quote detail (`app/pages/admin/properties/[id]/quotes/[quoteId].vue`) renders the tier badge, revision badge `v{N}`, expiry banner with warning/error tone, per-line `optional`/`discount`/`notes`/`category` chips, customer-visible-notes section, rejection-details panel, and three new actions: **Revise** (navigates to the new revision id), **Reject** (modal with `reasonCode` select + free-text reason), **Expire now** (calls `quote.expire()` if present else falls back to `quote.expireBatch({ organizationId })`). Quote list (`app/pages/admin/quotes/index.vue`) now groups by `revisionGroupId ?? id` sorted descending by `revisionNumber`; chevron toggle expands predecessor revisions (`data-testid="quote-revisions"` / `quote-row-predecessor`), each row exposes `quote-row-tier` + `quote-row-revision` chips. Work-order detail (`app/pages/admin/work-orders/[id].vue`) gains a schedule card (`scheduledStart`/`scheduledEnd` datetime-local + `priority` select + `estimatedHours` input + live `priority-chip` preview + `save-schedule-button` calling `workOrder.schedule()`), a `cost-rollup` card summing `estimated`/`actual`/`variance` from slot hours, per-slot `slot-window`/`slot-estimated-hours`/`slot-actual-hours` lines with **Start** and **Complete** buttons (Complete opens a modal capturing `actualHours` + `notes`), and a `change-orders-panel` listing rows with inline **Approve**/**Reject** plus a **Propose change order** modal. WO list (`app/pages/admin/work-orders/index.vue`) row now exposes `work-order-row-priority` chip, `work-order-row-scheduled` date, and a `work-order-row-due-in` badge with `overdue`/`warning`/`muted` tones. Invoice builder (`app/pages/admin/properties/[id]/invoices/new.vue`) gains a settings card: `terms-select` (`due_on_receipt|net_15|net_30|net_60|custom`) with a watcher that recomputes `dueDate` on change, a `deposit-input` (dollar string → cents on submit), and a `retainage-bps-input` (entered as 0–20%, persisted as bps capped at 2000). Invoice detail (`app/pages/admin/invoices/[id].vue`) gains a **Payments** panel (lists `invoicePayment.listForInvoice` rows with amount/method/reference/date), a **Record payment** modal posting through `invoice.recordPayment` (amount, method select, reference, received date, notes), a **Balance** summary card (`balance-total`/`balance-paid`/`balance-retainage`/`balance-remaining`), and a **Void** action with a reason modal (`invoice.voidInvoice`); the status chip and method labels now resolve via `useLabel().t(...)`. Invoice list (`app/pages/admin/invoices/index.vue`) adds `partial` and `voided` filter chips and a `invoice-row-days-past-due` badge for sent/partial rows past their `dueDate`/`dueAt`. Defaults file (`shared/labels/defaults.ts`) gained exhaustive `Record` dictionaries for `quote.tiers`, `quote.reject-reasons`, `work-order.priority`, `invoice.terms`, `invoice-payment.methods`, plus `status.quote.{rejected,expired}` and `status.invoice.{partial,voided}` (already present). `LabelNamespaceSchema` (`shared/contracts/label.ts`) was additively extended with 5 new namespaces — narrowly required because `tests/unit/labels.test.ts` enforces every DEFAULT_LABELS key resolve to a known namespace; no breaking change to existing label consumers. `BulwarkInput` types extended with `datetime-local` to host the WO schedule pickers. Tests: 5 new e2e specs (`tests/e2e/quote-tiers.spec.ts`, `quote-revise-reject.spec.ts`, `wo-schedule.spec.ts`, `invoice-partial-payments.spec.ts`, `change-order.spec.ts`) authored per the W2-3b contract. Unit suite remains green (24 files / 144 tests passing) and `vue-tsc --noEmit` is clean for every W2-3b file (verified per-file). Pre-existing workspace TS errors in the parallel property-depth slice (`attachments.vue`, `buildings/*`, `contacts.vue`, `photos.vue`, and their unit tests) are out of scope. Handoff: [`agents/handoffs/2026-05-15-W2-3b-q-wo-inv-ui.md`](agents/handoffs/2026-05-15-W2-3b-q-wo-inv-ui.md). Caveats: (1) the LabelNamespaceSchema additive change is the only contract touch — flagged because the brief said "do not edit contracts"; the alternative was to leave the new defaults unresolvable. (2) Five pre-existing TS errors in the untracked `app/components/property/PropertyDepthNav.vue` (introduced by a parallel slice) were fixed by renaming the destructured `t` to `tLabel` to avoid an auto-import shadow; behaviour is unchanged.

**Wave 2 — Quote / WO / Invoice Depth (EH-G / W2-3) — DONE.** Per [ADR-0020](agents/decisions/ADR-0020-quote-wo-invoice-depth.md), Bulwark's quote-to-cash spine now has depth: tiered quotes (`good|better|best|custom`) with revisions (`revisionGroupId` + `parentQuoteId` + `revisionNumber` — each revision keeps its own sequential `Q-YYYY-####`), per-quote `expiryDate` + idempotent `expire()` + `expireBatch({ now })`, structured `reject({ reason, reasonCode })`, per-line `optional` + `discountBps`, and `customerVisibleNotes` separate from internal notes. Work orders gain `scheduledStart`/`scheduledEnd` + `priority` (`urgent|high|normal|low`) + `estimatedHours`/`actualHours` per slot + `dispatchNotes`. Invoices gain partial-payments via new `invoice_payments` ledger table (`recordPayment` flips `sent → partial`; sum-to-total flips `partial → paid` and emits `invoiceMarkedPaid`; refuses payments against draft; `voidPayment` soft-deletes), `depositRequiredCents`/`depositReceivedCents`, `retainageBps`/`retainageReleasedCents`, `terms` (`on_receipt|net_15|net_30|net_60|custom`) + `dueDate`, and terminal `voidInvoice({ reason })` (refuses already-paid). New `change_orders` table with at-least-one-of `(workOrderId, invoiceId)` enforcement at service layer; approve appends a line to the linked invoice via hook, falls back to appending a WO note when only a WO is attached; idempotent. New page `app/pages/admin/dispatch.vue` — read-only 7-day kanban dispatch board (rows = subcontractors + Unassigned synthetic row; columns = next 7 days; cells render trade slots whose `scheduledStart` falls on that day; priority filter chips; `data-test="dispatch-cell-${row.id}-${dayIso}"` for E2E); nav entry added under Operations between Work orders and Quotes (role-gated to admin group). Migration `0005_shallow_adam_destine.sql` (generated + applied to `bulwark_dev`): creates tables `change_orders` + `invoice_payments`; creates enums `quote_tier`, `work_order_priority`, `invoice_terms`; extends `invoice_status` with `partial` + `voided`; adds 8 columns to `quotes`, 4 columns to `work_orders`, 8 columns to `invoices`. Nine new events on the bus: `quoteRevised`, `quoteRejected`, `quoteExpired`, `workOrderScheduled`, `invoicePartialPaid`, `invoiceVoided`, `changeOrderProposed`, `changeOrderApproved`, `changeOrderRejected`. Pipeline-default updates in `shared/pipelines/defaults.ts` — `INVOICE_PIPELINE` now includes `partial` (sortOrder 25, transitions to `paid|voided`) and `voided` (terminal); `sent.allowedTransitions` extended to `[partial, paid, voided]`; `draft.allowedTransitions` extended to `[sent, voided]`. Tests: 3 new unit specs (20 tests total, all pass): `tests/unit/quote-tiers.test.ts` (8), `tests/unit/invoice-payments.test.ts` (6), `tests/unit/change-orders.test.ts` (6). Typecheck clean for every W2-3 file. Handoff: [`agents/handoffs/2026-05-15-W2-3-q-wo-inv-depth.md`](agents/handoffs/2026-05-15-W2-3-q-wo-inv-depth.md). Known pre-existing failures (out of scope, verified via `git diff`): 2× `tryPreviewOpaqueInvite is not defined` in `tests/integration/auth.real.test.ts` (W2-4 territory) and 3× property auto-status transitions in `tests/integration/auto-status-transitions.test.ts` (pre-existing pipeline-check stub). Deferred to follow-up slice (load-bearing layers complete, contracts stable): quote-builder tier control + per-line optional/discount, quote-list revision grouping, WO-detail schedule card + change-orders panel, invoice-detail payments modal + Void action, invoice-new deposit/retainage/terms inputs, E2E specs.

**Wave 2 — Inspection Template Engine (EH-F / W2-2) — DONE.** Per [ADR-0019](agents/decisions/ADR-0019-inspection-templates-as-data.md), Bulwark is now driven by **templates-as-data** for every program's field-capture form. New schema (migration `0004_awesome_abomination.sql`): `inspection_templates` (per-org versioned, `is_builtin`), `inspection_template_sections` (ordered, optionally repeatable, `condition` jsonb), `inspection_template_fields` (`kind` enum across text/longtext/number/currency/boolean/select/multiselect/date/photo/signature/passfail/rating + `options` jsonb + `rules` jsonb), `inspections` (FK to properties + nullable `program_id`, status `draft|submitted|signed`), `inspection_responses` (unique on `(inspectionId, sectionInstanceKey, fieldSlug)` — repeatable sections key like `deck-0`, `deck-1`). Pure evaluator `shared/utils/inspection-evaluator.ts#evaluateInspection(template, responses)` returns `InspectionIssue[]` from six rule kinds (`required`, `must_be_true`, `must_be_false`, `must_be_one_of`, `min`, `max`); severity is `error | warning` only (critical/major derived later at the compliance layer). Mock + real services wired through both factories (`shared/mocks/factory.ts`, `server/utils/services-factory.ts`); the mock inspection service takes a `TemplateProvider` closure, the real inspection service owns a private `RealInspectionTemplateService` so `evaluate()` works without DI. Wildfire defaults live in `shared/inspection-templates/wildfire-defaults.ts` (8 sections: `zone_0`, `zone_1`, `zone_2`, `roof`, `vents`, `eaves`, `siding`, `deck` repeatable). Dynamic `app/components/inspections/InspectionForm.vue` renders any template — auto-saves on 800ms debounce + `onBeforeUnmount` flush, opens a SignaturePad modal on submit, displays evaluator issues post-sign. Two new property pages: `/admin/properties/[id]/inspection/new` (template picker → seed inspection → redirect) and `/admin/properties/[id]/inspection/[inspectionId]` (thin wrapper). Settings authoring at `/settings/inspection-templates` (section + field editor modal, role-gated to `ROLE_GROUPS.admin`); settings hub card counts bumped (org_admin 14→15, super_admin 15→16) and `tests/e2e/settings-matrix.spec.ts` updated. Legacy `assessment.vue` retained with a `legacy-assessment-banner` callout linking to the new flow. `server/services/compliance.real.ts#syncFromJob()` looks up the latest inspection for the property; if it has a `programId`, it calls `inspectionService.evaluate()` inside `try/catch` and logs results via audit (`metadata.kind='inspection_evaluator_used'`) — non-blocking, the compliance doc still ships on evaluator failure. Seed (`scripts/db-seed.mjs`) inserts the wildfire template per demo org with deterministic ids, wipes prior W2-2 rows for those orgs, then links each program via `UPDATE programs SET inspection_template_id = $tid WHERE id = $programId`. Tests: 2 new unit specs (`tests/unit/inspection-template-evaluator.test.ts` 3 cases + `tests/unit/inspection-template-bootstrap.test.ts` 3 cases) + 2 new e2e specs (`tests/e2e/inspection-dynamic.spec.ts` admin happy-path, `tests/e2e/settings-inspection-templates.spec.ts` editor → form rendering). Handoff: [`agents/handoffs/2026-05-15-W2-2-inspection-engine.md`](agents/handoffs/2026-05-15-W2-2-inspection-engine.md). Known gaps: compliance HTML still hardcodes assessment fields (evaluator → PDF wiring deferred); no template-versioning UI yet (built-ins mutate `version=1` in place). Pre-existing typecheck errors in invoices/audit/labels/quotes are unchanged by W2-2 — none of those files were touched.

**Wave 1B — State Continuity (EH-D / W1-4) — DONE.** Per [ADR-0017](agents/decisions/ADR-0017-domain-event-bus.md), Bulwark now has a typed in-process domain event bus, a single property-status subscriber that auto-transitions the parent property when its quotes/WOs/invoices change, a property-scoped audit timeline service method, and bi-directional navigation on every entity detail page. New code: `shared/events/bus.ts` (singleton via `globalThis['__bulwarkEventBus__']`; `defineEvent<T>()` brands payload type at compile time; `emit()` uses `Promise.allSettled` so a throwing handler never bubbles); `shared/events/catalog.ts` (flat catalog of propertyCreated, propertyStatusChanged, assessmentSigned, quoteSent/Accepted/Rejected/Expired, workOrderCreated/Started/Completed, complianceDocReady, invoiceSent/MarkedPaid/Overdue); `server/services/_subscribers/property-status.ts` (idempotent registration; `safeTransition()` audits its own failures as `metadata.kind='auto_status_transition_failed'`; reads property, idempotency-skips if already in target, calls a `resolvePipelineCheck()` stub — TODO swap point for W1-3 `statusPipelineService.canTransition()` — runs `withAudit` writing `state_change` row with `metadata.kind='auto_status_transition'`); `server/plugins/event-subscribers.ts` (Nitro plugin runs `registerPropertyStatusSubscribers()` at boot); `shared/mocks/audit.mock.ts` (minimal MockAuditService for test parity). Modified real services to emit AFTER `withAudit` commits: `property.real.ts` (propertyCreated on create), `quote.real.ts` (quoteSent on markSent, quoteAccepted on markAccepted), `work-order.real.ts` (workOrderCreated on create; workOrderStarted/Completed inside mutateSlot when envelope status flips), `invoice.real.ts` (invoiceSent on markSent when changed; invoiceMarkedPaid with paidAmountCents on markPaid when changed), `assessment.real.ts` (assessmentSigned on create — E4 has no separate sign step; documented inline), `compliance.real.ts` (complianceDocReady inside syncFromJob when transitioning to ready). Extended `IAuditService` with `timelineForProperty({organizationId, propertyId, limit≤500})`; `RealAuditService.timelineForProperty()` gathers child entity ids (quotes, work_orders, invoices, assessments, compliance_docs) via 5 parallel SELECTs then runs a single audit_log query filtered by entityType='property'+entityId=propertyId OR entityId IN (children) ordered DESC. Wired `audit: IAuditService` into `BulwarkServices` interface + both factories (real factory passes tenantResolver for firewall on list/timeline; record() stays internal-only so `withAudit` still works). UI: property detail (`app/pages/admin/properties/[id]/index.vue`) gains parallel rollup fetch (quotes/WOs/invoices/compliance/timeline), tab badge counts on Quotes/Work orders/Compliance/Invoices/Activity, "Linked work" rollup card on Overview showing active children with status pills, replacement of empty-state tab panels with linked lists, and a new **Activity** tab rendering a vertical timeline (date · entityType · metadata.kind · actor). Work-order detail (`app/pages/admin/work-orders/[id].vue`) gains a property crumb (replacing the work-orders list crumb) + "Linked work" card with `link-to-property` and `link-to-source-quote`. Invoice detail (`app/pages/admin/invoices/[id].vue`) gains the same crumb pattern + `link-to-property` / `link-to-source-work-order` / `link-to-source-quote`. Quote + compliance detail already had property-rooted breadcrumbs. Tests: 4 new unit specs (`tests/unit/event-bus.test.ts`: deliver, multi-subscriber, error isolation, unsubscribe — all passing) + 4 new integration specs (`tests/integration/auto-status-transitions.test.ts`: quoteAccepted→accepted, workOrderCreated→in_progress, quoteRejected gated by other active quotes, invoiceMarkedPaid→paid; skipped when `DATABASE_URL` absent) + 1 new e2e spec (`tests/e2e/state-continuity.spec.ts`: Activity tab renders; WO detail back-link visible). Handoff: [`agents/handoffs/2026-05-14-W1-4-state-continuity.md`](agents/handoffs/2026-05-14-W1-4-state-continuity.md). No new migrations — audit_log already exists and is the persistence layer. W1-3 contract dependency point: `resolvePipelineCheck()` in `server/services/_subscribers/property-status.ts` (~line 70) — when W1-3 ships, swap the stub body to `return statusPipelineService.canTransition` (imported from `../status-pipeline.real`).

**Wave 1B — Admin Config Part A (EH-H / W1-3) — DONE.** Per [ADR-0016](agents/decisions/ADR-0016-status-pipelines-as-data.md), status pipelines, trades, and org numbering/defaults are now runtime-editable per-tenant. New schema: `status_pipelines` (versioned per `organizationId`+`entityType`, only one `is_active=true` per tuple) + `status_pipeline_nodes` (slug, label_key, color, sort_order, is_initial, is_terminal, `allowed_transitions jsonb<string[]>`) + `trades` (mirrors programs: per-org slug unique, 6 built-ins seeded) + `org_settings` (singleton row: 3 number-format strings with `{year}` / `{seq:N}` tokens, default markup/tax bps, default quote-expiry / invoice-terms / SLA days). New contracts: `shared/contracts/status-pipeline.ts` exposing `canTransition({ organizationId, entityType, fromSlug, toSlug }) → { allowed, reason? }` — the W1-4 workflow-guard hook; `shared/contracts/trade.ts` with `BUILTIN_TRADES`; `shared/contracts/org-settings.ts` with `ORG_SETTINGS_DEFAULTS`. New pure helper `shared/utils/numbering.ts` (`formatSequentialNumber({format,year,seq})` + `buildLikePatternForYear()`) consumed by `quote.real.ts#nextQuoteNumber`, `work-order.real.ts#nextWorkOrderNumber`, `invoice.real.ts#nextInvoiceNumber` so the org's chosen format drives numbering. Defaults table-of-record in `shared/pipelines/defaults.ts` (property=13 nodes, quote=5, WO=5, invoice=3, compliance=5, job=4); unit tests assert every Zod status-enum value appears in the corresponding default pipeline so a fresh org never references an unknown slug. Mock + real services wired through both factories. Audit action enum constraint: pipeline save writes `action='update'` with `metadata.kind='pipeline_save'`; bootstrap writes `action='create'` with `metadata.kind='pipeline_bootstrap'`. Race-safe `org_settings.get()` uses `.onConflictDoNothing({ target: organizationId })` + re-read. Admin UI: `/settings/pipelines` (entity select toolbar, editable node table with up/down/remove, BulwarkMultiSelect transitions, Save → version bump), `/settings/trades` (programs-shape modal CRUD, builtin delete blocked), `/settings/numbering-defaults` (3 number-format inputs + 2 bps inputs + 4 day-count inputs). Settings hub gains 3 cards (14 admin / 15 super_admin); `tests/e2e/settings-matrix.spec.ts` card counts bumped accordingly. Tests: 17 new unit specs (`status-pipeline.test.ts` 9 + `trades.test.ts` 5 + `org-settings.test.ts` 3 svc + 5 numbering helper) + 3 new e2e specs (`settings-pipeline.spec.ts`, `settings-trades.spec.ts`, `settings-numbering.spec.ts`). Seed (`scripts/db-seed.mjs`) idempotently inserts the 6 default pipelines, 6 built-in trades, and the defaults `org_settings` row per demo org. Handoff: [`agents/handoffs/2026-05-14-W1-3-config-hub-part-a.md`](agents/handoffs/2026-05-14-W1-3-config-hub-part-a.md). Migration emitted as `0003_*.sql` via `pnpm db:generate`. W2 follow-ups flagged: (1) widen frozen `TradeSchema` Zod enum to read the trades catalog at runtime so custom trades reach the WO scaffolder, (2) consolidate the three duplicate `next<Entity>Number` helpers into an `INumberingService`.

**Wave 1A — CMS Labels & Branding (EH-B) — DONE.** Per [ADR-0014](agents/decisions/ADR-0014-cms-label-registry.md), Bulwark now has a per-tenant CMS for user-facing copy plus a branding singleton. New schema: `labels` (`organizationId`+`namespace`+`key`+`locale` unique, soft-delete, audit columns) + `org_branding` (1:1 with `organizations`: logo URL, primary/accent hex colors, footer text, support email/phone, license label, timezone, currency, date format). Code defines defaults in `shared/labels/defaults.ts` (exhaustive over every status / trade / role / program enum value — locked by `tests/unit/labels.test.ts`); the DB stores ONLY overrides. New contracts in `shared/contracts/label.ts` (`LabelNamespaceSchema` with 15 namespaces; `BrandingSchema` with `HexColor` regex; `ILabelService`). Mock (`shared/mocks/label.mock.ts`) and real (`server/services/label.real.ts`) services wired through both factories; real service upserts via `onConflictDoUpdate` on the unique tuple and synthesizes branding defaults on first read. New `useLabel()` composable (`app/composables/useLabel.ts`) exposes `t(namespace, key, fallback)` with a per-org `useState` cache and a lazy fire-and-forget reload; pure `resolveLabel()` helper extracted for vitest. Pilot integrations: `StatusBadge.vue` (every property status pill) and the WO trade chip + assignment modal on `/admin/work-orders/[id]`. Admin UI: `/settings/labels` (tabs per namespace family — Statuses / Trades / Roles / Programs / Emails / PDFs / CTAs — with per-row inline edit + bulk save + per-row reset) and `/settings/branding` (logo URL preview, color pickers, footer textarea, locale defaults). Settings hub gains 2 cards (Branding + Labels). Tests: 8 new unit specs (labels coverage + use-label priority rule) + 3 new e2e specs (`settings-labels.spec.ts`: admin rename → pipeline reflects, reset arc, field 403). Handoff: [`agents/handoffs/2026-05-14-W1-2-labels.md`](agents/handoffs/2026-05-14-W1-2-labels.md). Schema is already bundled into migration `0002_opposite_zuras.sql` (generated together with the W1-1 programs work); no fresh migration needed. The generic RPC dispatcher (`server/api/services/[service]/[method].post.ts`) already routes the new service — no per-service routes needed.

**Wave 1A — Programs / GC Generalization (EH-A) — DONE.** Per [ADR-0013](agents/decisions/ADR-0013-gc-generalization-programs.md), Bulwark is now an explicit **GC field-service platform** with a tenant-owned **Program** taxonomy. New schema: `programs` (`organizationId`+`slug` unique, `kind ∈ inspection_program|service_program`, builtin/active/sortOrder, `defaultTradeSlots` + `pricingDefaults` JSONB, nullable W2-2 template FKs) + `program_memberships` (entity ⟷ program many-to-many keyed on `(orgId, entityType, entityId)`). One seeded **Wildfire Retrofit** built-in per demo org; built-ins cannot be hard-deleted (service-level guard) or rebranded (contract omits slug/kind from update input). New contracts in `shared/contracts/program.ts` + `IProgramService`; mock (`shared/mocks/program.mock.ts`) and real (`server/services/program.real.ts`) services wired through both factories. Admin UI under `/settings/programs` (role gated to `ROLE_GROUPS.admin`); Settings hub gains a 9th card for org_admin / 10th for super_admin. Seed bumped (`scripts/db-seed.mjs`) to insert Wildfire per demo org idempotently. Tests: 7 new unit specs + 5 new e2e specs (`settings-programs.spec.ts`) + Settings matrix card counts updated (8→9, 9→10). Handoff: [`agents/handoffs/2026-05-14-W1-1-programs.md`](agents/handoffs/2026-05-14-W1-1-programs.md). Generates migration `0002_*` via `pnpm db:generate`.

**Wave 1A — Real-Backend Cutover (EH-C) — DONE.** Per [ADR-0015](agents/decisions/ADR-0015-real-backend-default.md), `BULWARK_BACKEND` defaults to `real` in both `nuxt.config.ts` and `playwright.config.ts`; `BULWARK_BACKEND=mock` is now an explicit opt-in for offline dev demos. Every domain in `BulwarkServices` has a verified real service + RPC route — no missing implementations blocking the cutover. Seed expanded with role-coverage personas (`org_manager`, `viewer`, full Acme matrix) plus one `ready`-state compliance doc; demo password unchanged (`BulwarkDemo!1`). New `pnpm db:reset` script drops the public schema, re-runs migrations, and re-seeds in one shot (dev-only; localhost guard). New canary spec `tests/e2e/happy-path-launch.spec.ts` walks client → property → assessment → quote (send/accept) → WO (assign/start/complete every slot) → compliance doc → invoice (send/markPaid) and runs first in CI. Handoff: [`agents/handoffs/2026-05-14-W1-5-real-cutover.md`](agents/handoffs/2026-05-14-W1-5-real-cutover.md). Migration steps for a fresh dev machine: (1) install Postgres 18, (2) `createuser bulwark_app` + `createdb bulwark_dev`, (3) populate `.env.local` (`DATABASE_URL`, `NUXT_SESSION_PASSWORD`, `JWT_SECRET`), (4) `pnpm db:migrate && pnpm db:seed`, (5) `pnpm dev`. Full instructions in [docs/RUNNING.md](docs/RUNNING.md). Two specs branching on `BULWARK_BACKEND === 'real'` still skip real mode (compliance preview + accept-invite) — preserved per the cutover contract; Wave 4 retires them.

E11-S13 — h3 RPC cutover scaffolding. **E11-S10 closed**: `puppeteer` 24.x + `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` added. New `server/jobs/r2.ts` lazy S3 client (R2 endpoint = `https://<account>.r2.cloudflarestorage.com`, region `auto`) with `uploadToR2()` + `signR2GetUrl()` (7-day default). New `server/jobs/render-compliance-doc.ts` is a pure HTML composer (escaped property + org + signature) — unit-tested without booting Chromium. New `server/services/compliance.real.ts` mirrors MockComplianceDocService: `create()` inserts the doc row in `generating` inside `withAudit`, enqueues a `compliance_doc` job via RealJobService with `payload.docId`, then stamps `jobId` back on the doc row; `syncFromJob()` reads the linked job and mirrors terminal state (succeeded→ready, failed→failed). The worker handler in `server/jobs/handlers/compliance-doc.ts` hydrates doc + property + org, renders HTML, lazy-loads Puppeteer (`headless: true`, `--no-sandbox` flags for container envs), generates a Letter PDF with 0.5in margins, uploads `compliance/<orgId>/<docId>.pdf` to R2, returns the signed URL. `BULWARK_PDF_STUB=1` opt-in returns a placeholder URL without Chromium/R2 — used by integration tests and any env missing creds. New `tests/unit/render-compliance-doc.test.ts` (HTML escaping + scope counts) + `tests/integration/compliance.real.test.ts` (full pipeline under stub). **93 tests passing** (37 unit + 56 integration; +3 new). **E11-S9 still closed**: pg-boss 12.18.2 + tsx; `pnpm run worker:jobs` is the Render Start Command. **E11-S6 + E11-S7 + E11-S8 + E11-S11 + E11-S12 + Standards still closed**: RealAssessmentService, RealQuoteService, RealSubcontractorService, RealWorkOrderService, RealInvoiceService, RealStandardsService, and RealApiKeyService are all live and fully tested against the local Postgres backend. Quote + invoice services recompute totals server-side via `computeQuoteTotals` (clients cannot lie about money). Quote/invoice numbers generated as `Q-YYYY-####` / `INV-YYYY-####` / `WO-YYYY-####` via org-scoped COUNT. Work-order `assignTrade` auto-bumps slot status `unassigned → assigned` when assigning, and back when clearing — single UPDATE rewrites the JSONB `tradeSlots` array (per the contract decision: no separate work_order_trades table at v1 scale). Audit-log enum constrained to `create|update|delete|state_change`; friendly action names like `assign_trade` go in `metadata.kind`. RealApiKeyService issues `bw_sk_…` 32-byte secrets, returns the raw secret EXACTLY ONCE on create, persists only the bcrypt hash + 12-char prefix; `verifySecret(orgId, raw)` helper for future server middleware. RealStandardsService one-row-per-tenant via INSERT ... ON CONFLICT, falls back to OREGON_DEFAULT_STANDARDS on first read.

**Epic E10 closed.** Field GC role gets a complete mobile-first surface: dashboard (`/field/dashboard`), properties list + detail (`/field/properties`, `/field/properties/[id]`), work-orders list (`/field/work-orders`), assessments list (`/field/assessments`), sync-queue stub (`/field/sync-queue`, offline strategy ADR deferred to Phase 2 PWA work), and shared profile (`/profile`). All field surfaces role-gated to `ROLE_GROUPS.field` via the `role` middleware; no `$` rendered anywhere on the field surface. Cross-links into the existing `/admin/properties/[id]/assessment` and `assessment-summary` pages (both already field-allowed via E4) keep field crews on a single capture flow without duplicating the assessment shell. New `tests/e2e/happy-path-field.spec.ts` walks dashboard → properties → property → assessment on a 390×844 viewport and asserts no `$` text on every field page. **128 chromium tests passing** (2 skipped) + 35 unit tests.

**Epic E9 closed.** Settings hub + 9 sub-routes live: 4 functional editors (Compliance Standards, API keys, Users viewer, Workflow viewer, Audit log viewer) and 3 informational stubs (Catalog, Templates, Feature flags) awaiting real-backend dependencies. Role-gated to `org_admin` + `super_admin`; `feature-flags` is `super_admin` only.

E9 closed: Settings hub + sub-pages. New contracts `shared/contracts/standards.ts` (`IStandardsService.get/save`, full-replace shape) and `shared/contracts/api-key.ts` (issue-once: `create()` returns `{ row, secret }`, `revoke()` is soft, prefix-only display). New `MockStandardsService` (synthesizes Oregon defaults until customised) and `MockApiKeyService` (`bw_sk_…` 32-char hex secret via `crypto.getRandomValues`, prefix = first 10 chars + ellipsis); both tenant-firewalled and skipping `Schema.parse` per the E3-S4 fixture-id lesson; both wired into `MockServiceFactory.cachedServices`. New pages: `/settings` (hub with 8 cards for `org_admin`, 9 for `super_admin`), `/settings/standards` (pill-style multi-select for compliant roof/siding/eaves/vents + defensible-space toggle, save calls full-replace), `/settings/api-keys` (issue-once banner with copy + dismiss, list with active/revoked pills, soft revoke), `/settings/company` (org summary card; full edit deferred to E11), `/settings/users` (membership viewer derived from fixture roster), `/settings/workflow` (read-only pipeline statuses + trades), `/settings/catalog` + `/settings/templates` (informational stubs), `/settings/audit-log` (read-only feed derived from row history across properties/quotes/work-orders/invoices), `/settings/feature-flags` (super_admin-only static list). New `tests/e2e/settings-matrix.spec.ts` (12 tests: hub-card count for org_admin and super_admin, role redirect for `field`, each stub page resolves with its testid, standards toggle+save without error, api-keys issue/dismiss/revoke flow, feature-flags 403 for org_admin). **127 chromium tests passing** (2 skipped) + 35 unit tests.

E8-S4 closed: Epic E8 closer. New `tests/e2e/happy-path-invoice.spec.ts` (single serial test) walks the full sponsor journey: open a WO → click Create invoice → land on the create flow with WO-derived line items pre-filled → set a unit cost → submit → land on the new invoice in `draft` → click Send (flips to `sent`) → click Mark paid (flips to `paid`) → navigate via the back-link (NOT `page.goto`, mock-state nav rule) to the list → click the Paid filter tab → assert the new invoice number is present. **115 chromium tests passing** (2 skipped) + 35 unit tests.

E8-S3 closed: create invoice from a work order. New `app/pages/admin/properties/[id]/invoices/new.vue` pre-fills one labor line per WO trade slot (description carried, unit cost editable) and one material line per WO material (qty + unit cost copied). Live totals via the shared `computeQuoteTotals` helper. Default 30-day due date. Submit calls `invoice.create()` then `router.push('/admin/invoices/[id]')` so client-mutated mock state survives navigation. WO detail header gains `create-invoice-cta` linking into the flow. New `tests/e2e/invoice-create.spec.ts` (2 tests). **114 chromium tests passing** (2 skipped) + 35 unit tests.

E8-S2 closed: invoice detail at `/admin/invoices/[id]`. Mirrors the quote-preview shape (header pill + date strip + line items + totals + optional notes). Status-driven actions: `draft` surfaces Send invoice (calls `invoice.markSent`), `sent` (incl. derived overdue) surfaces Mark paid (calls `invoice.markPaid`). Refresh after mutation flips the pill in place; toast confirms each transition. New `tests/e2e/invoice-detail.spec.ts` (3 tests covering send, mark-paid, and overdue pill rendering). **112 chromium tests passing** (2 skipped) + 35 unit tests.

E8-S1 closed: invoice list at `/admin/invoices` with segmented status filter (`All / Draft / Sent / Overdue / Paid`). New `shared/contracts/invoice.ts` defines a persisted `InvoiceStatus` enum (draft/sent/paid) plus an `InvoiceView` superset that adds `overdue` as a derived view via `deriveInvoiceView({status, dueAt, paidAt}, nowIso?)` (sent && unpaid && past `dueAt`). New `MockInvoiceService` (list/get/create/markSent/markPaid) reuses the cents-integer `computeQuoteTotals` helper by shape-mapping invoice line items into the quote line shape; tenant-firewalled; skips `InvoiceCreateInputSchema.parse` per the E3-S4 fixture-id lesson. Four seed invoices (one per view) bound to the seed property + WO so the list spec and future detail/closing specs are deterministic. Page mirrors the quotes-list pattern (`?status=` query, `BulwarkSegmentedControl`, `{ server: false }` asyncData) and applies the overdue derivation client-side. New `tests/e2e/invoices-list.spec.ts` (2 tests: row rendering + overdue narrowing). **109 chromium tests passing** (2 skipped) + 35 unit tests.

**Epic E7 closed.** All four stories landed: async-job primitives (S1) → generator UI (S2) → generating → preview + download (S3) → closing happy-path Playwright spec (S4).

E7-S4 closed: closing Playwright spec for Epic E7. New `tests/e2e/happy-path-compliance.spec.ts` (single serial test) walks the full sponsor journey: open the generator → pick a slot, sign, submit → land on doc detail mid-generation → wait for the polling cycle to surface ready → assert preview iframe + download href visible. Adds integration coverage on top of the per-story specs (`compliance-generator.spec.ts`, `compliance-preview.spec.ts`) so a regression in the create→enqueue→poll→render seam fails one named test. **107 chromium tests passing** (2 skipped) + 35 unit tests.

E7-S3 closed: “generating” → ready preview + download. New `app/composables/useComplianceDocPolling.ts` — setInterval-driven Vue-reactive wrapper around `IComplianceDocService.syncFromJob`, runs `tick()` once immediately then on a 400ms cadence (configurable) until terminal/timeout/error. Enforces `onBeforeUnmount(stop)` for clean teardown, surfaces `{ polling, error, start, stop }`. Doc detail page rewritten with three explicit branches: `generating` (centered spinner + reassuring copy), `ready` (sandboxed iframe preview + Download PDF anchor opening in a new tab), `failed` (retry CTA back to the generator). All branches share the doc summary card (id / signed by / slot count / signed at) and a single `compliance-doc-status[data-status]` selector for tests. New `tests/e2e/compliance-preview.spec.ts` (1 test): submit the generator → land on detail → wait for status to flip from `generating` to `ready` → assert iframe + download href visible. **106 chromium tests passing** (2 skipped) + 35 unit tests.

E7-S2 closed: compliance generator UI. New `shared/contracts/compliance.ts` (Zod) defining `ComplianceDocStatusSchema(draft/generating/ready/failed/cancelled)`, `ComplianceSignatureSchema` (signedByName + dataUrl + signedAt), `ComplianceDocSchema` (workOrderIds[], includedSlotIds[], jobId, resultUrl, error), `ComplianceDocCreateInputSchema`, and `IComplianceDocService { list, get, create, syncFromJob }`. New `MockComplianceDocService` writes the row in `generating`, calls `job.create({ kind: 'compliance_doc', payload })` to enqueue the async job, stores the returned jobId, and exposes `syncFromJob()` as an idempotent reconciler that mirrors job state onto the doc. Tenant-firewalled. Skips `ComplianceDocCreateInputSchema.parse` (E3-S4 fixture-id lesson, same as E6-S5/E7-S1). New `app/components/compliance/SignaturePad.vue` (canvas + pointer events; `pointerdown/move/up/cancel/leave`; DPR-aware backing store; `defineExpose({ clear })`; emits `update:modelValue` / `update:isEmpty`). New `app/pages/admin/properties/[id]/compliance/new.vue` (BulwarkBreadcrumbs + checklist of trade slots across the property's WOs with status pill + signer name input + SignaturePad + Generate button). Property detail Compliance tab now wires `tab-start-compliance-cta` to the new generator route. New stub `app/pages/admin/properties/[id]/compliance/[docId].vue` (S3 fleshes out the polling/preview surface). WO detail root gains `data-property-id` so test helpers can resolve the seed property. New `tests/e2e/compliance-generator.spec.ts` (2 tests using `locator.dispatchEvent('pointerdown'/move/up')` for canvas drawing). **105 chromium tests passing** (2 skipped) + 35 unit tests.

E7-S1 closed: async-job pattern primitives. New `shared/contracts/job.ts` (Zod) — `JobStatusSchema = enum(queued/running/succeeded/failed)`, `JobKindSchema = enum(compliance_doc)` (additive), `JobSchema` with `payload` (free-form record), `resultUrl`, `error`; `IJobService { create, get }`. New `MockJobService` schedules deterministic transitions via `setTimeout(runAfterMs)` then `setTimeout(completeAfterMs)`; `payload.__mockFail` flips terminal to `failed`; tenant-firewalled. New pure `shared/utils/asyncJob.ts` exports `pollUntilTerminal(getJob, opts)` — fetches once, then loops until terminal/timeout/abort with injectable sleep + `onTick` callback. New `tests/unit/async-job.test.ts` (7 tests using `vi.useFakeTimers`). **103 chromium tests passing** (2 skipped) + **35 unit tests** (3 new files of unit coverage for the job pipeline).

**Epic E6 closed.** All six stories landed: contracts (S1) → create-from-quote (S2) → sub assignment (S3) → progress updater (S4) → sub edit (S5) → closing happy-path (S6).

E6-S6 closed: closing Playwright spec for Epic E6. New `tests/e2e/happy-path-work-order.spec.ts` (single long serial test): build a draft quote → send → accept → click Create work order → submit the WO → assign first trade-matched sub → click Start work → assert the slot pill flips through `assigned` → `in_progress` and the envelope status follows the `deriveEnvelopeStatus` rule (`draft` → `scheduled` → `in_progress`). All navigation client-side. **103 chromium tests passing** (2 skipped) + 28 unit tests.

E6-S5 closed: subcontractor list rows are clickable and a new editable detail page lives at `/admin/subcontractors/[id]`. Form covers company / contact / email / phone / trades (BulwarkMultiSelect) / license number / license expires (BulwarkDatePicker, yyyy-mm-dd ↔ ISO conversion) / notes. Save calls `subcontractor.update` (new mock method) which patches in place, bumps `updatedAt`, and returns the merged row. Tenant firewall on every method. Trade list cleared → inline `Pick at least one trade.` validation, no save. New `tests/e2e/subcontractor-detail.spec.ts` (2 tests). **102 chromium tests passing** (2 skipped) + 28 unit tests.

E6-S4 closed: per-trade-slot progress updater. New `app/components/workorder/JobProgressUpdater.vue` (presentational; props `status`, `hasAssignment`, `busy`; emits `update`). Buttons render conditional on the current `TradeSlotStatus`: assigned→{Start, Block}, in_progress→{Complete, Block}, blocked→{Resume}, completed→{Reopen}, unassigned→needs-assignment hint. Photo capture is a disabled placeholder (E10 follow-up). Wired into the WO detail page slot rows; click handler calls `workOrder.updateTradeStatus(woId, slotId, next, orgId)` and the mock derives envelope status. New `tests/e2e/work-order-progress.spec.ts` (2 tests). **100 chromium tests passing** (2 skipped) + 28 unit tests.

E6-S3 closed: subcontractor assignment per trade slot. Each slot row on the WO detail page gains an Assign / Change inline button that opens a `BulwarkModal` listing trade-matched candidates from `MockSubcontractorService`. Picking a candidate calls `workOrder.assignTrade(woId, slotId, subId, orgId)` (idempotent), refreshes the bundle, fires a success toast, and the slot status flips unassigned↔assigned automatically (envelope-status derivation in the mock). A `Clear assignment` footer action covers the unassign path. New `tests/e2e/work-order-assign.spec.ts` (2 tests). **97 chromium tests passing** (2 skipped) + 28 unit tests.

E6-S2 closed: create work order from accepted quote. New page `/admin/properties/[id]/work-orders/new?quoteId=…` derives one trade slot per quote-line `sourceField` via a `FIELD_TO_TRADE` map (roofMaterial→roofing, sidingMaterial→siding, eaveType+ventType→eaves_vents, defensibleSpaceCleared→defensible_space; everything else collapses to general_labor). Editable trade list (add/remove + trade selector + description) + notes. Submit calls `workOrder.create` → `router.push('/admin/work-orders/[id]')`. Quote preview gained a new `accept-button` (status==='sent') and `create-work-order-cta` (status==='accepted'). New `quote.markAccepted` service method (idempotent; throws when called from non-sent status). New `tests/e2e/work-order-create.spec.ts` (2 tests). Adjusted `quote-preview.spec.ts` test 2 to assert `accept-button` after send (replaces the old already-sent-note assertion). **96 chromium tests passing** (2 skipped) + 28 unit tests.

E6-S1 closed: work-order + subcontractor contracts (Zod) wired into `BulwarkServices`. New `shared/mocks/subcontractor.mock.ts` (read-only list/get) and `shared/mocks/work-order.mock.ts` (list/get/create + assignTrade/updateTradeStatus, org-scoped `WO-YYYY-NNNN`, envelope-status derivation, full E2-S7 tenant firewall). Three seed subs across the six trades + one seed WO bound to the `accepted` fixture property so the detail page renders on first paint. New pages: `app/pages/admin/work-orders/index.vue` (list + `?status=` segmented filter), `app/pages/admin/work-orders/[id].vue` (detail with schedule, trades table, materials, notes), `app/pages/admin/subcontractors/index.vue` (minimal seed list so the sidebar link doesn't 404). All three pages `{ server: false }`. New `tests/e2e/work-order-detail.spec.ts` (3 tests). **94 chromium tests passing** (2 skipped) + 28 unit tests.

E5-S5 closed: closing happy-path for Epic E5. New `tests/e2e/happy-path-quote.spec.ts` (chromium-only, single long test, `mode: 'serial'`) threads property → assessment form → summary CTA → builder auto-populated → fill costs → save draft → preview → Send → sidebar to /admin/quotes → row visible as Sent → status filter narrows correctly. **Epic E5 complete** (S1–S5). **91 chromium tests passing** (2 skipped) + 28 unit tests.

E5-S4 closed: org-wide quotes list at `/admin/quotes` with status filter. Page renders persisted line items + totals + status pill; `Send` button calls `quote.markSent` (idempotent), surfaces a success toast, and flips the pill to Sent. Preview's `useAsyncData` runs `{ server: false }` so it always reads from the same client mock module the builder mutated. New `tests/e2e/quote-preview.spec.ts` (2 tests). **87 chromium tests passing** (2 skipped) + 28 unit tests.

E5-S2 closed: quote builder pre-populates from the latest non-compliant assessment. New `tests/e2e/quote-prepopulate.spec.ts` (2 tests). Builder watches `bundle.assessment.id`; if `route.query.from === 'assessment'` it auto-populates labor line items per recommended upgrade. Otherwise it shows a `prepopulate-banner` with a manual `prepopulate-button`. Property detail Quotes tab now ships a `tab-new-quote-cta` NuxtLink to `/quotes/new` (no query) so flow tests can navigate without `page.goto` (which would lose the client-side mock state). Summary page ships a `build-quote-from-assessment` NuxtLink (only when non-compliant) carrying `?from=assessment`. **85 chromium tests passing** (2 skipped) + 28 unit tests.

E5-S1 closed: quote builder + money helper + MockQuoteService. New `shared/contracts/quote.ts` (Zod), `shared/utils/money.ts` (formatCents / parseDollarsToCents / computeQuoteTotals), `shared/mocks/quote.mock.ts` (org-scoped Q-YYYY-NNNN sequence), `app/pages/admin/properties/[id]/quotes/new.vue` (mobile-first builder, live totals). New `tests/unit/money.test.ts` (13 tests) + `tests/e2e/quote-builder.spec.ts` (3 tests). **83 chromium tests passing** (2 skipped) + **28 unit tests**.

E4-S5 closed: Playwright happy-path closing Epic E4. New `app/pages/admin/properties/[id]/assessment-summary.vue` calls `evaluateCompliance(latest, OREGON_DEFAULT_STANDARDS)` client-side and renders a banner (✓ compliant / ! non-compliant) plus a one-row-per-flagged-field upgrades card with current value, required value, and ORS/OAR reference. Empty state deep-links to the form. **Important fix to E4-S2**: form's success redirect now goes to `/assessment-summary` via `router.push` (client navigation) instead of `?tab=assessment` because a full goto would hit a different SSR module instance with empty `rows[]`. Lesson logged in session memory: client-side mock mutations only survive client-side navigations — design redirects accordingly. New `tests/e2e/assessment-summary.spec.ts` (3 tests). **77 chromium tests passing** (2 skipped) + 15 unit tests.

## Recent Completions

| Date | Story | Notes |
|---|---|---|
| 2026-05-15 | W2-1 | **EH-E — Property Depth: buildings, sections, contacts, photos, attachments.** Five new tables + 7 new nullable columns on `properties` (`lot_size_acres`, `parcel_number`, `year_built`, `access_notes`, `gate_code`, `special_instructions`, `primary_contact_id`). Migration: [`server/db/migrations/0007_open_toro.sql`](server/db/migrations/0007_open_toro.sql). Four new contracts ([`building`](shared/contracts/building.ts), [`contact`](shared/contracts/contact.ts), [`property-photo`](shared/contracts/property-photo.ts), [`property-attachment`](shared/contracts/property-attachment.ts)) + `PropertySchema` extended with 7 new fields + `IPropertyService.getWithDepth(propertyId, organizationId)` returning a nested `PropertyDepth = { property, buildings: BuildingWithSections[], contacts, primaryPhotoUrl }`. Four new mocks (`__resetBuildingMock`, `__resetContactMock`, `__resetPropertyPhotoMock`, `__resetPropertyAttachmentMock`); factory ([`shared/mocks/factory.ts`](shared/mocks/factory.ts)) wires `propertyMock.attachDepthSources({ building, contact, photo })` after construction to resolve the depth-cycle without constructor coupling. Four new real services follow the existing `withAudit` + `assertSameTenant` pattern; `RealBuildingService.reorderSections` validates orderedIds enumerates the full active section set inside the transaction. `RealContactService.setPrimary` demotes siblings in the same tx. `RealPropertyPhotoService` + `RealPropertyAttachmentService` accept data URLs today with `// TODO(W3-1): swap for sealed-secret S3/R2 signed-URL upload` markers at every create(). Row mappers extended in [`server/services/_row-mappers.ts`](server/services/_row-mappers.ts) (lotSizeAcres coerces numeric→number; new mappers for buildings, sections, contacts, photos, attachments). Label registry extended: namespaces `property.tabs`, `building.kinds`, `contact.kinds`, `attachment.kinds` with `*_DEFAULTS` dictionaries in [`shared/labels/defaults.ts`](shared/labels/defaults.ts). New `PropertyDepthNav` sub-navigation component ([`app/components/property/PropertyDepthNav.vue`](app/components/property/PropertyDepthNav.vue)) lives at the top of every depth surface. Six new pages: [`buildings/index.vue`](app/pages/admin/properties/[id]/buildings/index.vue) (table + Add modal), [`buildings/[buildingId].vue`](app/pages/admin/properties/[id]/buildings/[buildingId].vue) (edit form + sections list with up/down reorder + Add modal), [`contacts.vue`](app/pages/admin/properties/[id]/contacts.vue) (list + add/edit modal + Set primary), [`photos.vue`](app/pages/admin/properties/[id]/photos.vue) (Tailwind gallery grid + data-URL upload + caption inline edit + filter chips by building), [`attachments.vue`](app/pages/admin/properties/[id]/attachments.vue) (table + upload + download). Overview tab augmented in [`index.vue`](app/pages/admin/properties/[id]/index.vue) with three new cards (`overview-property-details`, `overview-primary-contact`, `overview-primary-photo`) + a clickable buildings tile section (`overview-buildings`) — only rendered when underlying data is present. Three new unit specs ([`buildings.test.ts`](tests/unit/buildings.test.ts) 4 tests, [`contacts.test.ts`](tests/unit/contacts.test.ts) 2 tests, [`property-depth.test.ts`](tests/unit/property-depth.test.ts) 2 tests). New [`tests/e2e/property-depth.spec.ts`](tests/e2e/property-depth.spec.ts) walks the full happy path (building → section → primary contact → photo upload → overview reflects depth). ADR: [ADR-0018](agents/decisions/ADR-0018-property-depth.md). Handoff: [`agents/handoffs/2026-05-15-W2-1-property-depth.md`](agents/handoffs/2026-05-15-W2-1-property-depth.md). **152/152 unit tests pass.** **Typecheck clean (exit 0).** |
| 2026-05-15 | W2-6 | **EH-L — Design System Polish + Signature Primitive.** Three new UI primitives: [`BulwarkSignaturePad`](app/components/ui/BulwarkSignaturePad.vue) (canvas + pointer events, DPR-aware, explicit Save button, `defineExpose({ clear, save })`, `aria-label="Signature canvas"`), [`BulwarkTableSkeleton`](app/components/ui/BulwarkTableSkeleton.vue) (`animate-pulse bg-slate-200` shimmer; props `rows=5`/`cols=4`/`showHeader=true`; `aria-busy`+`aria-live`), and [`app/assets/print.css`](app/assets/print.css) (`@media print`: hides nav/aside/header buttons, flattens to B/W, inline URLs, 0.6in margins) wired into `nuxt.config.ts` `css[]`. Skeleton wired into the three highest-traffic list pages (`/admin/properties`, `/admin/quotes`, `/admin/work-orders`) with `*-loading` testids that don't collide with existing `*-empty`/`*-row` selectors. a11y baseline: skip-to-content link (`sr-only focus:not-sr-only`) as first interactive element in `app/layouts/default.vue`, `<main id="main-content" tabindex="-1" data-print-root>` wraps page content, `AppSidebar`/`AppTopBar`/`AppBottomNav` marked `.no-print`, `aria-label` added to `user-menu-button` + `org-switcher`. Icon adoption: `<BulwarkIcon>` adopted in `AppSidebar`/`AppBottomNav`/`AppTopBar`/`UserMenu`/`settings/index.vue` (~50 rendered glyphs); `shared/nav/nav.config.ts` icon names normalized to `ICON_NAMES` registry (4 remaps: `document`→`file-text`, `receipt`→`dollar-sign`, `cog`→`settings`, `sun`→`clock`). Inline-SVG audit of `app/**` found zero `<svg>` blocks outside `BulwarkIcon.vue` itself — adoption hit the 30–50 target via nav centralization. Tests: 14 new (`tests/unit/signature-pad.test.ts` 6, `tests/unit/table-skeleton.test.ts` 5) plus repaired `tests/unit/bulwark-icon.test.ts` (now imports `ICON_NAMES` from `icon-names.ts` instead of the unparseable `BulwarkIcon.vue` SFC — Vitest's node env has no `@vitejs/plugin-vue`); **144/144 unit pass**. NO new dep packages (W2-6 hard constraint). Legacy `app/components/compliance/SignaturePad.vue` retained — migrating callers is deferred (entrenched `update:isEmpty` semantics + e2e selectors). ADR: [ADR-0026](agents/decisions/ADR-0026-design-system-polish.md). Handoff: [`agents/handoffs/2026-05-15-W2-6-design-polish.md`](agents/handoffs/2026-05-15-W2-6-design-polish.md). No new typecheck errors introduced (pre-existing failures in `server/services/*.real.ts`, `shared/mocks/*`, `app/pages/admin/quotes/index.vue` unused-decls remain — out of scope for presentation-only slice). E2E smoke (`settings-matrix.spec.ts`) blocked locally by pre-existing `db-seed.mjs` FK violation (`programs ← inspection_templates`) — DB infrastructure issue unrelated to W2-6; flagged for follow-up. Deferred glyphs (W3-1 sprite expansion): `sun`, `document`, `receipt`, `cog`. |
| 2026-05-15 | W2-4 | **EH-H Part B — Admin Hub buildout.** Five new contracts (`user`, `feature-flag`, `provider-config`, `webhook`, `notification-subscription`) + filter/CSV extension to `audit`; five new mocks + five new `*.real.ts` services wired through `services-factory.ts`. Domain event bus gained `onAny()` wildcard registration ([`shared/events/bus.ts`](shared/events/bus.ts)). New `server/services/_subscribers/webhook-dispatcher.ts` fans every event with an `organizationId` payload out to subscribed webhooks; signs with HMAC-SHA256 over the raw body, header `X-Bulwark-Signature: hmac-sha256=<hex>`; retries 3× at 1s / 4s / 16s; records every attempt in `webhook_deliveries`. Six admin/profile pages: `settings/users.vue` (real invites + role/status mgmt), `settings/feature-flags.vue` (per-org overrides), `settings/providers.vue` (per-kind Zod-validated config), `settings/webhooks.vue` (issue-once secret + multi-event-type), `settings/audit-log.vue` (filterable + CSV export), `profile/notifications.vue` (matrix of channels per event). `RealAuthService.acceptInvite` extended to consume opaque hex tokens from `pending_invites` (sha256 hash lookup) with JWT fallback for legacy invites; seeds `NOTIFICATION_DEFAULTS` after membership creation. Schema additions: `pending_invites`, `feature_flags`, `provider_configs`, `webhooks`, `webhook_deliveries`, `notification_subscriptions` (FK to users now `ON DELETE CASCADE`). Migration: `server/db/migrations/0006_salty_prism.sql`. Four new unit specs ([`tests/unit/user-admin.test.ts`](tests/unit/user-admin.test.ts), [`feature-flags`](tests/unit/feature-flags.test.ts), [`webhooks`](tests/unit/webhooks.test.ts), [`notification-subscriptions`](tests/unit/notification-subscriptions.test.ts)) — 19 new tests. ADRs: [ADR-0021](agents/decisions/ADR-0021-admin-hub-users-and-providers.md), [ADR-0022](agents/decisions/ADR-0022-webhooks-subscriber.md). Handoff: [`agents/handoffs/2026-05-15-W2-4-admin-hub-finish.md`](agents/handoffs/2026-05-15-W2-4-admin-hub-finish.md). **Typecheck clean. 130/130 unit tests pass.** Known sec-debt: provider configs + webhook secrets are stored unsealed (W3-1 KMS column). E2E specs deferred to a follow-up slice. |
| 2026-05-14 | EH-C | **Wave 1A — Real-Backend Cutover.** `BULWARK_BACKEND` defaults flipped from `mock` to `real` in `nuxt.config.ts` (line 103) and `playwright.config.ts` (line 51) per [ADR-0015](agents/decisions/ADR-0015-real-backend-default.md). Real-service surface audited — every `BulwarkServices` entry has a verified `server/services/*.real.ts` + RPC route (no missing implementations). Seed expanded with role-coverage personas (`org_manager`, `viewer`, full Acme matrix: ana/mike/felix/sam/val @acme.demo) + 1 `ready`-state compliance doc on the seed property. New `pnpm db:reset` script (`scripts/db-reset.mjs`) drops + remigrates + reseeds in one shot, dev-only / localhost-guarded. New canary smoke spec `tests/e2e/happy-path-launch.spec.ts` walks client → property → assessment → quote (send/accept) → WO (assign/start/complete every slot) → compliance doc → invoice (send/markPaid); runs first in CI. New [docs/RUNNING.md](docs/RUNNING.md) documents the fresh-machine local-dev path. App plugin (`app/plugins/services.ts`) header rationale refreshed; no behaviour change. Two specs still skip real mode (`compliance-preview.spec.ts`, `happy-path-compliance.spec.ts`, `auth-recovery.spec.ts` accept-invite) — preserved per cutover contract; flagged for Wave 4. `app/pages/settings/users.vue` still imports from `shared/mocks/fixtures` — flagged for W2-4 (needs real `IUserService.list`). Handoff: [`agents/handoffs/2026-05-14-W1-5-real-cutover.md`](agents/handoffs/2026-05-14-W1-5-real-cutover.md). Migration steps for fresh dev: see RUNNING.md; TL;DR `createdb bulwark_dev` → set `DATABASE_URL` in `.env.local` → `pnpm db:migrate && pnpm db:seed` → `pnpm dev`. |
| 2026-05-06 | E8-S4 | Epic E8 closer. New `tests/e2e/happy-path-invoice.spec.ts` (single serial test): open a WO → Create invoice → land on create flow with WO-derived line items pre-filled → set unit cost → submit → invoice in `draft` → Send (flips to `sent`) → Mark paid (flips to `paid`) → navigate via back-link to the list (mock-state nav rule — NOT `page.goto`) → click Paid filter → assert new invoice number present. **Epic E8 complete** (S1–S4). **115 chromium / 2 skipped + 35 unit**. |
| 2026-05-06 | E8-S3 | Create invoice from WO. New `app/pages/admin/properties/[id]/invoices/new.vue` pre-fills one labor line per trade slot (editable cost) + one material line per material (qty + unit cost copied); live totals via `computeQuoteTotals`; default 30-day due date; submit → `invoice.create()` → `router.push('/admin/invoices/[id]')`. WO detail header gains `create-invoice-cta`. New `tests/e2e/invoice-create.spec.ts` (2 tests). **114 chromium / 2 skipped + 35 unit**. |
| 2026-05-06 | E8-S2 | Invoice detail at `/admin/invoices/[id]` mirroring the quote-preview shape (header + date strip + line items + totals + optional notes). Status-driven actions: `draft` → Send invoice (`invoice.markSent`); `sent` (incl. derived overdue) → Mark paid (`invoice.markPaid`). Refresh flips the pill in place. New `tests/e2e/invoice-detail.spec.ts` (3 tests). **112 chromium / 2 skipped + 35 unit**. |
| 2026-05-06 | E8-S1 | Invoice list with status + overdue filter. New `shared/contracts/invoice.ts` (persisted `InvoiceStatus` = draft/sent/paid; derived `InvoiceView` adds `overdue` via `deriveInvoiceView`), `shared/mocks/invoice.mock.ts` (list/get/create/markSent/markPaid; reuses `computeQuoteTotals` via shape-map; tenant-firewalled; skips create-input parse per fixture-id lesson). Four seed invoices spanning every view bound to the seed property + WO. New `app/pages/admin/invoices/index.vue` mirrors the quotes-list pattern with `BulwarkSegmentedControl` (`All / Draft / Sent / Overdue / Paid`); overdue computed client-side. New `tests/e2e/invoices-list.spec.ts` (2 tests). **109 chromium tests passing** (2 skipped) + 35 unit tests. |
| 2026-05-05 | E7-S4 | Epic E7 closer. New `tests/e2e/happy-path-compliance.spec.ts` (single serial test): open the generator → pick a slot, sign, submit → land on doc detail mid-generation → wait for the polling cycle to surface `ready` → assert preview iframe + download href. Threads `compliance-generator.spec.ts` + `compliance-preview.spec.ts` so an integration regression (create→enqueue→poll→render) fails one named test. **Epic E7 complete** (S1–S4). **107 chromium tests passing** (2 skipped) + 35 unit tests. |
| 2026-05-05 | E7-S3 | Compliance “generating” → preview + download. New `app/composables/useComplianceDocPolling.ts` (Vue-reactive setInterval wrapper around `complianceDoc.syncFromJob`; immediate first tick + configurable interval/timeout; `onBeforeUnmount(stop)`). Doc detail page now renders three explicit states: `generating` (spinner + reassuring copy, testid `compliance-generating`), `ready` (sandboxed `<iframe>` preview at `compliance-preview-iframe` + Download PDF anchor at `compliance-download-button`), `failed` (retry CTA). Status surfaces on `compliance-doc-status[data-status]` for state assertions. New `tests/e2e/compliance-preview.spec.ts` (1 test driving the full async transition). **106 chromium tests passing** (2 skipped) + 35 unit tests. |
| 2026-05-04 | E7-S2 | Compliance generator UI. New `shared/contracts/compliance.ts` (Zod) — `ComplianceDocStatusSchema(draft/generating/ready/failed/cancelled)`, `ComplianceSignatureSchema(signedByName + dataUrl + signedAt)`, `ComplianceDocSchema(workOrderIds[], includedSlotIds[], jobId, resultUrl, error)` + `IComplianceDocService { list, get, create, syncFromJob }`. New `MockComplianceDocService` writes the row in `generating`, calls `job.create({ kind: 'compliance_doc', payload })` to enqueue the async job, stamps the returned jobId, and exposes `syncFromJob` (idempotent reconciler mirroring job state onto the doc). Tenant-firewalled; skips `ComplianceDocCreateInputSchema.parse` (E3-S4 fixture-id lesson). New `app/components/compliance/SignaturePad.vue` (HTML canvas + pointer events with DPR-aware backing store, `defineExpose({ clear })`, emits `update:modelValue` + `update:isEmpty`; defensive `setPointerCapture` for synthesized test events). New `app/pages/admin/properties/[id]/compliance/new.vue` (breadcrumbs + checklist of all trade slots across the property's WOs with status pill + signer name + SignaturePad + Generate button; `{ server: false }`). Property detail Compliance tab now wires `tab-start-compliance-cta` to the new route. Stub `[docId].vue` page accepts the post-create redirect (S3 fleshes out the polling/preview). WO detail root gains `data-property-id`. New `tests/e2e/compliance-generator.spec.ts` (2 tests using `locator.dispatchEvent('pointerdown'/move/up')` for canvas drawing). **105 chromium tests passing** (2 skipped) + 35 unit tests. |
| 2026-05-04 | E7-S1 | Async-job primitives. New `shared/contracts/job.ts` (Zod) — `JobStatusSchema(queued/running/succeeded/failed)`, `JobKindSchema(compliance_doc)` (additive enum), `JobSchema` with `payload` record, `resultUrl`, `error`; `IJobService { create, get }`. New `MockJobService` schedules deterministic transitions via `setTimeout(runAfterMs)` → `setTimeout(completeAfterMs)`; `payload.__mockFail: true` opts into the failed terminal; tenant-firewalled. Wired into `BulwarkServices.job` + `MockServiceFactory.cachedServices`. New pure `shared/utils/asyncJob.ts` exports `pollUntilTerminal(getJob, { intervalMs, timeoutMs, signal, onTick, sleep })` — fetches once, then loops until terminal/timeout/abort. Throws `Job not found` (tick 0), `AbortError`, `Job polling timed out`, or `Job vanished mid-poll`. New `tests/unit/async-job.test.ts` (7 tests using `vi.useFakeTimers` + the default setTimeout-based sleep so fake timers control polling cadence). **103 chromium tests passing** (2 skipped) + **35 unit tests**. |
| 2026-05-04 | E6-S6 | Epic E6 closer. New `tests/e2e/happy-path-work-order.spec.ts` (single long serial test): build draft quote on a fresh property → send → accept → Create work order → submit → assign first trade-matched sub → Start work → assert slot pill `assigned`→`in_progress` and envelope status `draft`→`scheduled`→`in_progress` per `deriveEnvelopeStatus`. All navigation client-side. **Epic E6 complete** (S1–S6). **103 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E6-S5 | Subcontractor list rows clickable; editable detail page at `/admin/subcontractors/[id]` (BulwarkInput / BulwarkMultiSelect / BulwarkDatePicker / BulwarkTextarea covering company, contact, email, phone, trades, license number, license expires, notes). New `subcontractor.update(id, patch, orgId)` mock method — partial merge, bumps `updatedAt`, tenant-firewalled, skips full-schema re-parse to honor non-RFC4122 fixture ids (E3-S4 lesson). Date input ↔ ISO conversion in the page (`yyyy-mm-dd` → `T00:00:00.000Z`). Inline `Pick at least one trade.` validation when the multi-select is cleared. New `tests/e2e/subcontractor-detail.spec.ts` (2 tests). **102 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E6-S4 | Per-trade-slot job progress updater. New `app/components/workorder/JobProgressUpdater.vue` (presentational; props `status`, `hasAssignment`, `busy`; emits `update`). State machine: assigned→{Start, Block}, in_progress→{Complete, Block}, blocked→{Resume}, completed→{Reopen}; unassigned shows `progress-needs-assignment` hint and no actions. Photo capture rendered as a disabled placeholder ("Add photo (coming soon)") for E10 follow-up. Wired into the WO detail page slot rows; click handler calls `workOrder.updateTradeStatus(woId, slotId, next, orgId)`, refreshes the bundle, and the mock derives envelope status (e.g. all completed → WO completed). New `tests/e2e/work-order-progress.spec.ts` (2 tests — assigned roofing slot transitions to in_progress→completed; unassigned defensible_space slot shows the needs-assignment hint and hides Start). **100 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E6-S3 | Sub assignment per trade slot. WO detail page slot rows gain an inline Assign/Change button that opens a `BulwarkModal` filtered to subs whose `trades[]` includes the slot trade. Picking calls `workOrder.assignTrade(woId, slotId, subId, orgId)` and the mock derives envelope status. Footer Clear-assignment action handles unassign. New `tests/e2e/work-order-assign.spec.ts` (2 tests). **97 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E6-S2 | Create WO from accepted quote. New `/admin/properties/[id]/work-orders/new` page reads `?quoteId=…`, derives trade slots from each line item's `sourceField` (roofMaterial→roofing, sidingMaterial→siding, eaveType+ventType→eaves_vents, defensibleSpaceCleared→defensible_space; fallback general_labor). Editable trade list (add/remove, trade selector, description), notes. Submit → `workOrder.create` → `router.push('/admin/work-orders/[id]')`. Quote preview adds `accept-button` (sent) and `create-work-order-cta` (accepted). New `quote.markAccepted` (idempotent; rejects non-sent transitions). New `tests/e2e/work-order-create.spec.ts` (2 tests). Adjusted `quote-preview.spec.ts` test 2 to assert `accept-button` instead of `already-sent-note`. **96 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E6-S1 | Work-order + subcontractor contracts (Zod) added to `BulwarkServices`. `WorkOrderStatusSchema = enum(draft/scheduled/in_progress/completed/cancelled)`; `TradeSlotStatusSchema = enum(unassigned/assigned/in_progress/completed/blocked)`; `TradeSchema = enum(roofing/siding/gutters/eaves_vents/defensible_space/general_labor)` with `TRADE_LABEL` map. `WorkOrderSchema` carries `propertyId`, `quoteId`, `workOrderNumber`, `tradeSlots[]`, `materials[]`, schedule window. New `shared/mocks/subcontractor.mock.ts` (read-only) + `shared/mocks/work-order.mock.ts` with `assignTrade(woId, slotId, subId\|null, orgId)` and `updateTradeStatus(woId, slotId, status, orgId)` + automatic envelope-status derivation. Both wired into `MockServiceFactory.cachedServices`. Fixtures: 3 subs (Roof King, Cascade Siding, Firebreak Clearing) + 1 seed WO `WO-2026-0001` on the `accepted` property with two trade slots. New pages `/admin/work-orders` (segmented `?status=` filter), `/admin/work-orders/[id]` (schedule + trades table with status pills + sub names + materials + notes), `/admin/subcontractors` (minimal seed list). All `{ server: false }`. New `tests/e2e/work-order-detail.spec.ts` (3 tests). **94 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E5-S5 | Closing happy-path for Epic E5. New `tests/e2e/happy-path-quote.spec.ts` (chromium-only, `serial`, single long test): pick property → Assessment tab → fill non-compliant assessment (wood_shake, vinyl, enclosed eave, ember_resistant vent, defensible toggled) → summary banner → build-quote-from-assessment link → builder auto-populated with ≥ 2 line items → fill unit costs → save draft → preview shows draft status + Q-YYYY-NNNN number → Send → sidebar Quotes link → list shows the row as Sent → Sent filter keeps it → Draft filter shows empty state. **Epic E5 complete** (S1–S5). **91 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E5-S4 | Org-wide quotes list at `/admin/quotes`. Filters via `?status=` (segmented control: All / Draft / Sent / Accepted / Rejected / Expired). Each row links to the existing preview at `/admin/properties/[id]/quotes/[quoteId]`; the loader fetches the matching properties in parallel for address rendering. Empty state CTA points back at `/admin/properties`. Page is `{ server: false }` so it stays consistent with builder/preview client-side mock state. New `tests/e2e/quotes-list.spec.ts` (3 tests). To navigate cross-page during the multi-quote tests we route through pipeline NuxtLinks + property detail Quotes tab — `page.goto` between mutations would reset the mock module. **90 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E5-S3 | Quote preview + mock Send. New `app/pages/admin/properties/[id]/quotes/[quoteId].vue` renders persisted quote (number, line items with kind / qty / unit, totals breakdown including markup% and tax%, optional notes) and a status pill. `Send` button calls `quote.markSent`, refreshes the bundle, fires a success toast (`Sent to {client.fullName}`), and is replaced by the already-sent note. Page uses `useAsyncData(…, { server: false })` so the fetch always reads from the same client mock module the builder mutated. New `tests/e2e/quote-preview.spec.ts` (2 tests — draft preview + send transition). **87 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E5-S2 | Quote builder pre-populates from non-compliant assessment. New `tests/e2e/quote-prepopulate.spec.ts` (2 tests — auto-populate via summary CTA carrying `?from=assessment`, manual `prepopulate-banner` + button when arriving without the query). Builder watches `bundle.assessment.id` and `populateFromAssessment()` writes one labor line item per `requiredUpgrade` with field-label-aware descriptions. `populated` ref hides the banner once items exist. Summary page gains `build-quote-from-assessment` NuxtLink for the non-compliant branch. Property detail Quotes tab gains a `tab-new-quote-cta` button → `/quotes/new` (no query) so flow tests can route there via NuxtLink (the mock-state rule forbids `page.goto` after a client-side mutation). **85 chromium tests passing** (2 skipped) + 28 unit tests. |
| 2026-05-04 | E5-S1 | Quote builder scaffolding. New Zod contract `shared/contracts/quote.ts` (Quote, QuoteLineItem, QuoteTotals, QuoteCreateInput, IQuoteService). New pure helpers `shared/utils/money.ts` — `formatCents`, `parseDollarsToCents`, `computeQuoteTotals` (markup-then-tax order, half-away-from-zero rounding). New `MockQuoteService` (module-level rows[], tenant firewall, org-scoped `Q-YYYY-NNNN` sequence, idempotent `markSent`). New `app/pages/admin/properties/[id]/quotes/new.vue` builder — mobile-first stack of line-item cards, live `formatCents` totals card, save-draft routes to (yet-empty) preview URL. New `tests/unit/money.test.ts` (13 tests) + `tests/e2e/quote-builder.spec.ts` (3 tests — render, live totals, persist+route). **83 chromium tests passing** (2 skipped) + **28 unit tests**. |
| 2026-05-04 | E4-S5 | Closing happy-path for Epic E4. New `tests/e2e/happy-path-assessment.spec.ts` (chromium-only, single long test): pick a property → Assessment tab CTA → form with non-compliant wood_shake roof → summary shows non-compliant banner + `upgrade-roofMaterial` row containing `OAR 629-044-1030` → breadcrumb back → Assessment tab preview reflects the same non-compliant state. **Epic E4 complete** (S1–S5). **80 chromium tests passing** (2 skipped) + 15 unit tests. |
| 2026-05-04 | E4-S4 | Property detail Assessment tab wired to evaluator. `[id]/index.vue` now imports `evaluateCompliance` + `OREGON_DEFAULT_STANDARDS` and adds a `compliance` computed; the tab slot conditionally renders an EmptyState + `tab-start-assessment-cta` (no assessment) or a colored banner + `tab-view-summary-link` deep-link to the summary (assessment exists). Re-run link points back at the form. New `tests/e2e/property-assessment-tab.spec.ts` (2 tests — empty CTA, post-submit banner via NuxtLink-only navigation honoring the mock-state rule). **79 chromium tests passing** (2 skipped) + 15 unit tests. |
| 2026-05-04 | E4-S3 | Assessment summary page. New `[id]/assessment-summary.vue` calls the pure evaluator + renders banner + upgrades table with ORS/OAR refs. Form redirect changed to client-side `router.push('/assessment-summary')` so the mock service singleton survives navigation. New `tests/e2e/assessment-summary.spec.ts` (3 tests — empty-state CTA, compliant green banner, non-compliant flag with correct standard ref). **77 chromium tests passing** (2 skipped). |
| 2026-05-04 | E4-S2 | Assessment form + mock service. New `IAssessmentService` + `MockAssessmentService` (singleton via factory, tenant-firewalled). New nested route `app/pages/admin/properties/[id]/assessment.vue` (BulwarkSelect material pickers + BulwarkToggle defensible-space). Restructured `[id].vue` → `[id]/index.vue` to support the nested page. New `tests/e2e/property-assessment-form.spec.ts` (3 tests — renders, blocks-empty-submit, saves+routes-back). **74 chromium tests passing** (2 skipped). |
| 2026-05-04 | E4-S1 | Pure-function compliance evaluator + Vitest unit tests. New `shared/contracts/assessment.ts` (Assessment, ComplianceResult, ComplianceStandards). New `shared/utils/compliance.ts` exports `evaluateCompliance(input, standards)` and `OREGON_DEFAULT_STANDARDS` (BULWARK_TECH §8). 9-case unit suite covers every failure mode, multi-fail aggregation, tenant override (E9 wiring), and optional defensible-space. **15 unit tests passing**. |
| 2026-05-04 | E3-S7 | Happy-path Playwright closing Epic E3. One long integration test threads intake → pipeline → status menu → detail hub → tab switch → breadcrumb back. New `tests/e2e/happy-path-property.spec.ts`. **Epic E3 complete** (S1–S7). **71 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-04 | E3-S3 | Inline status-change menu (substituted for original drag-drop). New `PropertyStatusMenu.vue` (kebab button → 13-item menu with current marker, click-outside + Escape close). Card + list forward `change-status` up to the page, which calls `property.updateStatus()` + `refreshNuxtData()`. Drag-drop sugar can layer on later without changing the data path. New `tests/e2e/property-status-menu.spec.ts` (3 tests). **70 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-04 | E3-S6 | Clients list at `/admin/clients` (closes the sidebar's previously-404 link) and client detail at `/admin/clients/[id]`. Detail shows contact, notes, and the client's linked properties (filtered client-side from a pageSize-200 list call — fixture-scale only). 404 empty state on unknown ids. Property hub Overview already deep-links here. New `tests/e2e/client-detail.spec.ts` (4 tests). **67 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-04 | E3-S5 | Property detail hub at `/admin/properties/[id]`. Eight tabs via `BulwarkTabs`; Overview pulls property + linked client through a single `useAsyncData` join. Other tabs are `EmptyState` placeholders pointing at their owning epic (E4 assessment/photos, E5 quotes, E6 work orders, E8 compliance, E9 invoices, E10 notes). `?tab=` query is the source of truth (deep-link friendly). 404 empty state for unknown ids. New `tests/e2e/property-detail.spec.ts` (4 tests). **63 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-04 | E3-S4 | Property intake form. New `app/pages/admin/properties/new.vue` (the kanban moved into `properties/index.vue` so the folder can host the new route). Form uses `BulwarkInput` / `BulwarkSelect` / `BulwarkTextarea`; validates with `PropertyCreateInputSchema.omit({ organizationId: true })` (org id is injected from session, not user-controlled — fixture orgIds are intentionally non-RFC-4122). On success: `property.create()` → `refreshNuxtData('properties-${orgId}')` → `router.push('/admin/properties')`. New `tests/e2e/property-intake.spec.ts` (3 tests — render, empty-form validation, valid submission lands on pipeline). Cold-start fixes: added `waitForLoadState('networkidle')` to auth persona quick-pick, both list-toggle tests, and the intake submit tests. **E3-S3 deferred** (drag-drop is risky; will return after E3-S5/S6). **59 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-03 | (planning) | BUILD_PLAN.md, all 14 epic files, 11 ADRs, BUILD_STATUS.md created. Demo frozen. |
| 2026-05-03 | E0-S1 | CONTRACTS.md + UI-CONTRACTS.md skeletons + CONVENTIONS root pointer landed. |
| 2026-05-03 | E0-S2 | Nuxt 3 scaffold at repo root. Verified booting on http://localhost:3000. |
| 2026-05-03 | E0-S3 | tailwind.config.ts + tokens.css + main.css aligned to BULWARK_STYLE_GUIDE §2–§4. |
| 2026-05-03 | E0-S7 | playwright.config.ts (3 device projects), vitest.config.ts, drizzle.config.ts. |
| 2026-05-03 | E0-S8 | .github/workflows/ci.yml — typecheck/lint/unit + Playwright. |
| 2026-05-04 | E0-S4 | Drizzle schemas: organizations, users (+ roleEnum), memberships, clients, properties (+ propertyStatusEnum 13 values). Remaining domain tables land just-in-time per epic. |
| 2026-05-04 | E0-S5 | Zod contracts: `_shared`, `auth`, `property`, `client`, `services` barrel — service interfaces are the single source of truth between mock + real impls. |
| 2026-05-04 | E0-S6 | Mock services + factory + Nuxt plugin + composables: `useService('property')`, `useSession()`. Defaults to FIXTURE_USER_ADMIN signed in for dev DX (real auth lands E2-S1). 13 property fixtures (one per status), 5 clients, 3 users. |
| 2026-05-04 | E1-S1 | Single AppLayout (sidebar + topbar + bottom nav). `nav.config.ts` is the only nav source. ADR-0005 enforced: only `default.vue` renders nav. nuxt.config `components: { pathPrefix: false }` so nested folders don't prefix names. Admin dashboard placeholder + role-aware index redirect. **10 Playwright tests passing** across Chromium/Mobile Safari/Pixel (4 nav-shell desktop+mobile tests, 6 smoke tests). 3 UI primitives shipped: BulwarkButton, BulwarkCard, StatusBadge. |
| 2026-05-04 | E1-S2 + E1-S3 + E1-S4 + E1-S5 (combined) | 22 UI primitives shipped: Input, Textarea, Select, MultiSelect, Toggle, PassFailToggle, DatePicker, SegmentedControl, SearchField, FilePicker, KpiCard, JobCard, Avatar, EmptyState, Skeleton, Pagination, Breadcrumbs, Modal, Drawer, Tabs, Stepper, ToastHost. `useToast()` composable + `<BulwarkToastHost />` mounted in default layout. `/dev/ui` playground page. Tailwind tokens extended (status.{success,warning,error,info}, surface.muted, primary.700, height.input, borderRadius.input, spacing.bottom-nav/topbar). Fixed AppBottomNav inline `display:grid` defeating `md:hidden`. **19 Playwright tests passing** (smoke + nav-shell + 9 ui-primitives spec running serial). |
| 2026-05-04 | E2-S1 | Cookie-backed MockAuthService (replaces broken module-level state — SSR + client now share session via `bulwark.mock.persona` cookie). New `useAuth()` composable (login/logout/loading/error). `/login` page with form + dev persona quick-pick. `auth.global.ts` middleware redirects unauthed traffic to `/login?next=...` with status 302. AppTopBar gains inline Sign Out (full UserMenu in E2-S5). Test helpers `signIn` / `signInAsAdmin` / `signOut` added; existing specs updated to seed admin cookie in beforeEach. **23 Playwright tests passing**. |
| 2026-05-04 | E2-S2 | Forgot/reset password + accept-invite (stateless base64url tokens with kind + exp; trivial to verify). New pages `/forgot-password`, `/reset-password`, `/accept-invite` (all `layout: false`). `useAuth` extended with `requestPasswordReset` / `resetPassword` / `previewInvite` / `acceptInvite`. Reset path forces fresh sign-in (logout + bounce to `/login?reset=ok`) — matches future real backend session-revoke. Accept-invite does a hard navigation into `/admin/dashboard` to sidestep a Nuxt 3.21 SPA-vs-`layout: false` transition race. Mock `lookup()` now synthesizes a SessionUser when the cookie email isn't yet in `userByEmail` (mirrors how a real backend trusts a signed session cookie). **23 chromium tests passing** + 6 new auth-recovery tests. |
| 2026-05-04 | E2-S3 | Role-based middleware + `/403` page. New `usePermissions` composable exports role-group constants (`ROLE_GROUPS.admin`, `.field`, `.sub`, etc.) plus `hasAnyRole` / `isAdmin` / `isField` / `isSub` / `isSuperAdmin`. New named middleware `app/middleware/role.ts` reads `definePageMeta({ requiredRoles })` and bounces non-matches to `/403` (302). `/403` page is styled (layout-less, calls `ensureLoaded` itself since auth.global skips public routes), shows the user's actual role and offers a role-aware "Go to my dashboard" button + Back. `/admin/dashboard` and `/admin/properties` opted in. **28 chromium tests passing** (1 skipped) including 5 new role-guard tests; auth-spec adjusted to use admin persona for `?next=/admin/properties`. |
| 2026-05-04 | E2-S4 | Org switcher. New super_admin fixture (`sasha@bulwark.platform`) with memberships in Bulwark Demo Co. + Acme Restoration LLC. Active-org override persisted via second cookie `bulwark.mock.activeOrg`. `MockAuthService.currentUser` applies the override (and falls back gracefully if it points to a revoked membership); `switchActiveOrg` validates membership and writes the cookie; `login` / `logout` clear the override. New `/org-switcher` page + topbar widget upgraded to a NuxtLink for multi-org users (static chip for singletons). `useAuth.switchActiveOrg`. **30 chromium tests passing** (1 skipped) including 2 new org-switcher tests. |
| 2026-05-04 | E3-S2 | Pipeline list view (mobile) + FT-12 segmented kanban/list toggle. New `PipelineList.vue` renders status-grouped flat list with sticky section headers; empty sections are omitted (the kanban already shows the full status set). `useState('properties.view')` persists the choice across nav; `onMounted` flips narrow viewports (`< 768px`) to list. New `tests/e2e/properties-pipeline-list.spec.ts` (4 tests). **56 chromium tests passing** (2 skipped) + 6 unit tests. |
| 2026-05-04 | E3-S1 | Properties pipeline kanban (desktop) at `/admin/properties`. New `PipelineColumn` + `PropertyCard` components under `app/components/property/`. Server-rendered via `useAsyncData` (pageSize 100), grouped client-side by status, all 13 columns rendered in fixed left-to-right UX order with empty-state line for empty columns. Header shows total + `New property` deep-link CTA. New `tests/e2e/properties-pipeline.spec.ts` (5 tests — column count, populated statuses, empty status, deep-link href, CTA visible). **53 chromium tests passing** (1 skipped) + 6 unit tests. |
| 2026-05-04 | E2-S8 | Persona × route matrix. New `tests/e2e/persona-matrix.spec.ts` data-driven over a `CASES` table covering `(persona, route)` → `{allow, forbid, redirect-login}`. 10 cases: {admin, super_admin, field, sub, anonymous} × {`/admin/dashboard`, `/admin/properties`}. Adding a new protected route is a one-line table edit. Epic **E2 complete**. **48 chromium tests passing** (1 skipped) + 6 unit tests. |
| 2026-05-04 | E2-S7 | Tenant firewall in MockServiceFactory. New `shared/mocks/tenant.ts` (`TenantContext`, `TenantResolver`, `TenantViolationError`, `assertSameTenant`). `MockPropertyService` + `MockClientService` constructors accept an optional resolver and call `assertSameTenant(this.tenantResolver, organizationId)` at the top of every method — cross-tenant requests throw before any data access. Factory wires the default resolver via new `MockAuthService.resolveTenantSync()` so the singleton mock services pick up the active-org override per-request. New `tests/unit/tenant-firewall.test.ts` (6 tests) covers: cross-tenant property.list/get throws, cross-tenant client.list throws, same-tenant succeeds, missing resolver short-circuits, null resolver short-circuits. **38 chromium tests passing** (1 skipped) + **6 unit tests**. |
| 2026-05-04 | E2-S6 | Styled 404 / 500 error pages. New `app/error.vue` branches on `error.statusCode`: 404 card (`data-testid="not-found-card"`) and generic server-error card (`data-testid="server-error-card"`), both with `home-button` + `back-button`. Home calls `clearError({ redirect: '/' })`; Back uses `window.history.length > 1` then `router.back()` after `clearError()`, else falls through to home. New `/dev/throw` page (public per `auth.global` `/^\/dev(\/\|$)/`) deliberately throws a 500 for the spec. Removed `@pinia/nuxt` from `nuxt.config.ts` modules and uninstalled `pinia` / `@pinia/nuxt` — its payload plugin's `shouldHydrate` calls `obj.hasOwnProperty(skipHydrateSymbol)` on null-prototype objects in the `NuxtError` payload and throws, masking every 4xx as a 500. No stores in the app yet — re-enable when needed. New `tests/e2e/error-pages.spec.ts` (3 tests). **38 chromium tests passing** (1 skipped). |
| 2026-05-04 | E2-S5 | UserMenu dropdown. New `app/components/nav/UserMenu.vue` (`data-testid` `user-menu-button`, `user-menu-panel`, `user-menu-switch-org`, `logout-button`) replaces the inline Sign Out chip in `AppTopBar`. Click-to-toggle, Escape closes, `mousedown` listener attached only while the menu is open and removed on close. `Switch organization` link only renders for multi-membership users. Existing tests that asserted the bare `logout-button` updated to assert `user-menu-button` (or to first open the dropdown). New `tests/e2e/user-menu.spec.ts` with 5 tests. **35 chromium tests passing** (1 skipped). |

## Next Up

1. **E6-S1** — Work order schema + service + assignment.
2. E6-S2 — Work order detail + status transitions.
3. E6-S3+ — Subcontractor wiring, photo evidence, completion flow.

## Verified locally

- `pnpm install` clean (Nuxt 3.21.4, Nitro 2.13.4, Vite 7.3.2, Vue 3.5.33, Tailwind 3).
- `pnpm exec nuxt prepare` generates `.nuxt/tsconfig.json`.
- `pnpm dev` serves http://localhost:3000 — `Bulwark` wordmark + four portal cards render.

## Known follow-ups (defects to fix in next session)

- `pnpm typecheck` script needs `vue-tsc` installed; will add in E0-S5.
- Custom ESLint rules per ADR-0005 / ADR-0008 (banned hex outside tokens.css, banned `<aside>` outside nav, top-of-file comment block enforcement) — bootstrap rule files in E0-S5/S6 follow-up.
- `pnpm-lock.yaml` must be committed for CI's `--frozen-lockfile`.

## Blockers

_None._

## Open Questions for Sponsor

_None._
